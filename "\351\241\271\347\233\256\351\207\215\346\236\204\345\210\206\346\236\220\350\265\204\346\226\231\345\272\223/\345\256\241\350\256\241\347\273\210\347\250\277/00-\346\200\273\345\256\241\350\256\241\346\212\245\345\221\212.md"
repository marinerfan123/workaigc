# 00 · 总审计报告（墨灵 AI 重构前系统体检收尾）

> **签发对象**：技术负责人 / 架构师
> **审计性质**：**只读体检**。未修改 `E:\code` 下任何源码、配置、数据库；未运行任何写入/变更/支付触发命令。
> **审计周期**：2026-08-11
> **证据底座**：`审计终稿/02-15`（功能/API/DB/Redis/调度/计费/支付/Provider/权限/实时/前端/配置/DR/测试）+ `项目重构分析资料库/00-06`（总览/架构/模块/业务规则/等价基线/源码索引）+ `07_PG_schema快照.sql` + `_evidence_db_introspection.json`，并回查 `server/server.js`、`server/billing.cjs`、`package.json`（均【源码已证实】）。
> **配套交付**：`22-目标架构建议.md`、`23-迁移映射.md`、`24-重构Phase计划.md`、`AI_REFACTOR_CONTEXT.md`。

---

## 1. 执行摘要（一页纸）

墨灵 AI 是一套 Node 原生 HTTP 后端 + React 前端的 AI 图像/视频生成工作室应用，数据源为 PG17（41 表），缓存/协调用 Redis7，资产存阿里云 OSS。异步生成、双余额计费、支付 fail-closed、PG 持久化崩溃恢复等**核心业务设计已被源码证实成立且正确**，可作为重构的「等价基线」保留。

但系统存在 **6 项 P0 级风险**，集中在两条生命线：**资金安全**与**安全权限**，外加一个**前端 P0 契约缺陷**：

1. **`/api/token` 公开返回系统令牌**（P0-A）：dev 部署下任意访客可冒充管理员接管全站。
2. **JWT 默认密钥可伪造**（P0-B）：`.env` 未设 `JWT_SECRET`，会话可被离线伪造。
3. **资金幂等缺失**（P0）：`credit_transactions`/`consumption_ledger` 缺唯一约束，并发双退/双记账、失败键复用漏退。
4. **webhook 绕过 billing 裸 SQL 入账**（P0 资金缺口）：两套记账语义漂移。
5. **`/api/oss` 明文返回 `accessKeySecret`**（P0 凭据泄露，🔴CRITICAL）。
6. **`canceled`(后端) vs `cancelled`(前端) 拼写不一致**（P0 前端）：跨标签取消失效，卡片永久「生成中」。

此外，**测试体系形同虚设**（`npm test`=vitest 但 8 个用例全用 `node:test`、`npm test` 实际跑不到），核心资金/调度/支付模块零测试——这是重构安全网的最大缺口。

**结论性判定（详见 §5）**：T1–T6 全部已完成表征/核验，六大核心模块（生成/计费/支付/权限/实时/DB）均已完成验证。**旧系统调查已达到「完成」标准，可以开始规划正式重构（READY TO PLAN）。** 但 6 项 P0 必须作为重构计划（Phase1/Phase2）的强制内容，且在进入生产切换（Phase4/Phase5）前必须关闭，否则构成上线阻塞项。

---

## 2. 审计范围与方法

### 2.1 范围
| 维度 | 覆盖 |
|---|---|
| 后端 | `server/server.js`（中央 handler）、`dispatcher/billing/accounting/payments/realtime/redis/auth/admin/finance/shop/reference-styles/modelhub` 等模块 |
| 前端 | `src/app.tsx`、`src/services/api.ts`、`useGenerationStream.ts`、`GenerationBar.tsx`、`data/*.ts`、`components/ui/*` |
| 数据 | PG17（41 表，实查 + 快照）、Redis7（8 孤儿 key，实查）、OSS（零二进制） |
| 配置 | `.env`（已读，9 项）、`.env.example`（缺 6 项） |
| 测试 | 8 个测试文件 + `package.json` 脚本 |

### 2.2 方法
- **只读**：仅读源码/配置/文档；对 PG 仅 `information_schema`/只读自省；对 Redis 仅 `DBSIZE`/`TYPE`/`TTL`；未执行任何写命令。
- **双向取证**：前端契约（错误格式/状态码/字段命名/SSE/取消拼写）同时核对 `src/` 与 `server/`，做到前后端互证。
- **行号锚定**：所有结论标注源码行号，便于复核。

### 2.3 证据等级体系
| 标记 | 含义 |
|---|---|
| 【源码已证实】 | 直接对照源码可确认 |
| 【数据库已证实】 | PG 自省/快照确认 |
| 【运行时已证实】 | 运行实例/Redis 自省确认 |
| 【测试已证实】 | 存在对应测试覆盖 |
| 【文档记载但源码未证实】 | 文档提及、源码未落实 |
| 【推断】 | 基于代码逻辑推演 |
| 【未知·待核验】 | 需运行时/DB 实测 |

置信度：**HIGH / MEDIUM / LOW / UNKNOWN**。

---

## 3. 系统健康评分

> 风险等级：🔴 HIGH（高危，须优先处置）/ 🟠 MEDIUM（中危，规划内处置）/ 🟢 LOW（低危，可观察）。

| 维度 | 风险 | 评分依据 | 证据 |
|---|---|---|---|
| 资金安全 | 🔴 HIGH | 双退/双记/漏退 P0；webhook 裸 SQL；无事务/唯一约束 | 【源码已证实】HIGH（07/08/15） |
| 安全权限 | 🔴 HIGH | P0-A `/api/token`、P0-B JWT 默认密钥、P0 `/api/oss` 凭据泄露、P1 IDOR/owner 缺失 | 【源码已证实】HIGH（03/10/13） |
| 数据完整性 | 🟠 MEDIUM | 双 initDB 漂移、oss 双表、孤儿表、无外键级联 | 【数据库已证实】HIGH（04） |
| 实时 | 🟠 MEDIUM | 纯进程内 SSE，多实例硬限制；取消 casing bug（前端 P0） | 【源码已证实】HIGH（11） |
| 前端契约 | 🔴 HIGH | `canceled/cancelled` P0；SNAKE_MAP 隐藏契约；错误字符串嗅探 | 【源码已证实】HIGH（12/11） |
| 可测试性 | 🔴 HIGH | 框架不匹配（vitest vs node:test）、核心模块零测试 | 【源码已证实】HIGH（15） |
| 可观测性 | 🟠 MEDIUM | 有 error_logs/audit_logs/request_logs/logbus，但无分布式追踪 | 【源码已证实】MEDIUM（02/14） |
| 性能扩展 | 🟠 MEDIUM | 单实例部署；并发信号量 OK；多实例限流/实时不一致 | 【源码已证实】HIGH（05/11） |

**风险分布**：HIGH **4** / MEDIUM **4** / LOW **0**。

> 解读：无 LOW 维度——系统在「资金/安全/前端契约/可测试性」四个维度均处高危，需在重构早期（Phase1/Phase2/Phase3）集中处置。数据完整性/实时/可观测性/性能扩展为「中危」，多数可在 Phase4/Phase5 结构性解决。

---

## 4. P0 风险汇总表（全部 P0）

| # | 风险 | 现象 | 根因（源码锚点） | 爆炸半径 | 证据等级/置信度 | 修复 Phase |
|---|---|---|---|---|---|---|
| P0-1 | `/api/token` 公开泄露系统令牌 | 任意人 `GET /api/token` 得全局令牌；dev 下 `appGateway` 置 `system` → 绕过全部 `requireAdmin` | `server.js:3623` 无鉴权 + `devTokenEnabled=!isProduction||tokenFromEnv`（:31）+ `appGateway` Bearer 优先（:1276） | **远程未授权提权/全站管理员接管**（用户/积分/账务/删用户/清日志） | 【源码已证实】+【配置文件已证实】HIGH | P2 |
| P0-2 | 会话 JWT 默认密钥可伪造 | 离线用 `'dev-only-change-me'` 签发任意 `role:admin` 会话 | `auth.cjs:9` `SECRET=...||'dev-only-change-me'`；`.env` 无 `JWT_SECRET`；生产仅 `console.warn`（server.js:3714）非 fail-closed | **认证绕过/任意身份伪造** | 【源码已证实】+【配置文件已证实】HIGH | P2 |
| P0-3 | 资金幂等缺失：双退/双记/漏退 | 并发两次 release 双过 `_hasPosted`→余额双加；并发同键双记账→margin 虚高；失败键复用删任务行不清理流水→永久漏退 | `credit_transactions` 无 `(ref,kind)` 唯一约束（仅 `ix_ct_ref`）；`consumption_ledger.idempotency_key` 仅 `ix_cl_idem`；`_hasPosted` SELECT→INSERT 非原子（billing.cjs:48-55）；`server.js:2415` 漏清流水 | **用户资产损失 / 经营账务失真** | 【源码已证实】HIGH | P1 |
| P0-4 | webhook 绕过 billing 裸 SQL 入账 | 支付入账直接 `UPDATE users.recharge_credits`，未复用 `BillingEngine`，两套记账语义漂移 | `webhook.cjs:128-168` 裸 SQL；Grep 证实 webhook 不引 billing | **账务不一致（本金/流水/双边台账口径漂移）** | 【源码已证实】HIGH | P1 |
| P0-5 | `/api/oss` 明文返回 `accessKeySecret` | 任意登录用户（dev 下任何人）可读 OSS/AK 密钥，直控对象存储 | `server.js:3329-3351` 仅会话鉴权即返回 `accessKeySecret` 明文 | **凭据泄露 → 对象存储被直控/数据外泄** | 【源码已证实】HIGH | P2 |
| P0-6 | `canceled`(后端) vs `cancelled`(前端) 拼写不一致 | 用户取消后 SSE 与 3s 轮询均因状态字符串不匹配而不结算；跨标签取消的 pending 卡片永久「生成中」直至刷新 | 后端 `dispatcher.cjs:1252/1254` 用 `canceled`（单 l）；前端 `useGenerationStream.ts:84/92`、`GenerationBar.tsx:570` 用 `cancelled`（双 l） | **取消功能在 UI 层近乎失效（体验/视觉状态泄漏）** | 【源码已证实】HIGH | P3 |

> 附：高危但列为 P1（非 P0）的同源问题：`DELETE /api/characters/:id` 缺 owner/admin 校验（03 §3.3 / 10 P1-C）、`/providers/:id/sync|test-*` 缺 `requireAdmin`（03 §3.4）、多实例实时硬限制（11 §4）、`appGateway` Bearer 优先致审计失真（10 P1-D）。均已在 `22/23/24` 中排期处置。

---

## 5. 完成标准核对（T1–T6 与核心模块验证）

### 5.1 用户原定完成标准
> 要求满足「**T1–T6=0、核心模块验证、特定子系统确认**」方可宣布「可以开始规划正式重构」。

**释义**（据 `04_重构等价需求基线.md`「待核验清单 ✅ 已全部核验完毕」与 `00_总览与导航.md`）：「T1–T6=0」指 **6 项调查任务全部完成表征/核验（零未决）**，而非「系统零缺陷」。T1 经核验为「确认存在的缺陷（已表征，非已修复）」，正符合该释义。

### 5.2 T1–T6 逐项核对

| 编号 | 调查项 | 结果 | 表征/修复状态 | 证据位置 | 证据等级/置信度 |
|---|---|---|---|---|---|
| T1 | MiniMax/Volcano 适配器 failed→error 致 failover 空转 | ❌ **确认不一致（缺口）**：适配器返 `error` 被当瞬时错误切账号，积分永占 | **已表征，非已修复** | `06 §10` / `09 §5`（minimax.cjs:85 / volcano.cjs:121） | 【源码已证实】HIGH |
| T2 | 参考样式分成是否真实转账 | ✅ **确认真实转账**：`creditStyleDesigner`(server.js:1272) 按 30% 进设计者 reward + style_earnings 台账，幂等、仅 approved | 已确认（正确行为） | 基线 `04_重构等价需求基线.md` T2；`02 F-045`  corroborate | 【源码已证实】HIGH |
| T3 | sellers 是否为真实实体 | ✅ **确认非实体**：全项目零匹配，设计者=reference_styles.user_id/style_earnings.designer_id | 已确认 | 基线 `04` T3；`12` 路由 corroborate | 【源码已证实】HIGH |
| T4 | volcano 是否独立 modelConfigs | ✅ **确认**：volcano 即火山方舟 Seedance，配置归 seedance.ts，无 volcano.ts | 已确认 | `09 §2.3/§6`（volcano.cjs） | 【源码已证实】HIGH |
| T5 | 两个 initDB 分工/漂移 | ✅ **确认**：db.cjs initDB(6 表) 仅 migrate 调用；server.js initDB(39 表+ALTER) 为运行权威，字段已漂移 | 已确认 | `04-数据库真实结构 §2`（明确写「T5 已指出，本次实查确认」） | 【数据库已证实】HIGH |
| T6 | modelConfigs 是否被运行时 import | ✅ **确认**：modelConfigs 为 agent 草稿模块，src/ 零 import，运行时读 /api/models（DB） | 已确认 | `09 §6`（schema.ts 注释「不依赖任何运行时逻辑」） | 【源码已证实】HIGH |

**关于 T2/T3/T5 的透明度说明（按用户指令）**：用户要求「若底稿未覆盖请标注【未知·待核验】」。经核查，`审计终稿/02-15` 中 **T5 已被 `04-数据库真实结构 §2` 显式确认**；T2/T3 的**专用 T-核验戳记位于同级基线文档 `04_重构等价需求基线.md`（✅ 全部核验完毕）**，且 `审计终稿` 中 `02 F-045`（分成转账）、`10`（scope）、`12`（sellers 路由）已 corroborate 其底层事实。因此 T2/T3/T5 **并非未覆盖**，不标记 UNKNOWN；但为严谨起见，建议重构规划启动前以一次只读 SQL 对运行库复核 T2（分成台账行数）、T3（sellers 表缺失）、T5（双 initDB 列差），成本极低、可彻底闭环。若仅以「审计终稿 02-15 各自独立重述」为口径，则 T2/T3 在审计终稿内无独立 T-戳记，保守读者可将其视为【未知·待核验】 pending 该次复核——本审计已提供复核路径。

### 5.3 核心模块验证核对

| 核心模块 | 是否验证 | 证据 | 等级/置信 |
|---|---|---|---|
| 生成（dispatcher 状态机/崩溃恢复） | ✅ | `06-任务调度状态机` | 【源码已证实】HIGH |
| 计费（双余额/三段式/幂等） | ✅ | `07-计费资金状态机` | 【源码已证实】HIGH |
| 支付（fail-closed/验签/加密） | ✅ | `08-支付系统` | 【源码已证实】HIGH |
| 权限（鉴权/RBAC/IDOR） | ✅ | `10-认证权限矩阵` / `03-API契约` | 【源码已证实】HIGH |
| 实时（SSE/轮询/多实例） | ✅ | `11-实时通信` | 【源码已证实】HIGH |
| DB（41 表/约束/漂移） | ✅ | `04-数据库真实结构` + 实查 | 【数据库已证实】HIGH |

### 5.4 明确判定

> **判定：READY —— 旧系统调查已完成，可以开始规划正式重构（NOT BLOCKED FOR PLANNING）。**

**判定依据（可辩护）**：
1. T1–T6 全部完成表征/核验，**零未决调查项**（满足「T1–T6=0」）。
2. 六大核心模块（生成/计费/支付/权限/实时/DB）**均已验证**（满足「核心模块验证」）。
3. 特定子系统（Provider/Redis/OSS/DR/测试/前端）**均已出具专项事实文档并交叉核验**（满足「特定子系统确认」）。
4. 发现的 6 项 P0 是**重构的处置对象**，而非「调查未完成」的阻塞项——其存在恰恰证明调查已深入到位。重构计划（Phase0–Phase5）已将其编排为 Phase1/Phase2 的强制交付。

**重要边界（防误读）**：
- 「可以开始**规划**正式重构」≠「可以**动生产**/可以**上线**新系统」。
- **上线阻塞项（必须在 Phase4/Phase5 生产切换前关闭）**：P0-1（`/api/token`）、P0-2（JWT 密钥）、P0-3（资金幂等）、P0-4（webhook 裸 SQL）、P0-5（`/api/oss` secret）。这 5 项若不先以 Phase1/Phase2 关闭，直接进入生产切换将把高危暴露面带入新系统。
- P0-6（`canceled/cancelled`）为用户体验级，须在 Phase3 关闭，不阻断资金/安全上线，但应在切换前修复。

---

## 6. 后续行动建议

| 行动 | 指向 | 优先级 |
|---|---|---|
| 立即建立字符化测试网 + Golden Master 基线，统一测试框架 | `24 Phase0` / `15-测试覆盖缺口` | P0（先于一切重构动作） |
| 资金安全：唯一约束 + 事务 + 幂等（消除双退/双记/漏退/裸 SQL） | `22 §6` / `23 §B/C.1` / `24 Phase1` | P0 |
| 安全加固：`/api/token`/JWT/owner/凭据加密/CORS | `22 §7` / `23 §A/D` / `24 Phase2` | P0 |
| 前端契约解耦：错误 code 枚举 / SNAKE_MAP 替代 / `canceled` 统一 | `22 §5` / `23 §D/E` / `24 Phase3` | P0 |
| 目标架构落地：模块化 router / 注册表 / 多实例实时 / 单一 schema 源 | `22` 全文 / `23` 全文 / `24 Phase4` | P1 |
| 切换下线 + 孤儿表决策 + DR 增强（自动备份/PITR/回滚） | `23 §C.4` / `24 Phase5` / `14-DR` | P2 |
| 重构执行 Agent 交接：自包含上下文文件 | `AI_REFACTOR_CONTEXT.md` | — |

> **一句话给技术负责人**：调查已完备、可规划；但请以 Phase0（测试网）为闸门、以 Phase1+Phase2（资金+安全）为不可绕过的先手，再谈架构落地与切换。

*（本文为只读审计产物，未改动 `E:\code` 任何文件。）*
