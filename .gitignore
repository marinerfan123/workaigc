# Dependencies
node_modules/
*/node_modules/

# Production builds
dist/
*/dist/
build/
*/build/

# Environment files
.env.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
/logs/

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?

# === Secrets / runtime credentials (P0: repo is PUBLIC, never leak these) ===
.env
.env.*
!.env.example
server/data/oss.json
server/data/.api_token
server/data/media.json
server/data/media-uploads/
server/data/*.json
!server/data/oss.json.example

# === Agent memory (contains conversation history, must not be public) ===
.workbuddy/

# === Build output (covers dist/build2 used by this app) ===
dist/
build/
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary folders
tmp/
temp/
.playwright-cli/

# Lock files (keep package-lock.json for consistent installs)
yarn.lock
pnpm-lock.yaml

# Cache files
.eslintcache
*.tsbuildinfo

# swc wasm
.swc

# Agent files
.agent/

# ===== 运行时机密（绝不入库） =====
.env
.env.*
!.env.example
server/data/*.json
server/data/.api_token
server/data/media-uploads/
logs/
*.log

# ===== 上下文/对话记忆（含历史） =====
.workbuddy/

# ===== 凭据脚本（含真实 API Key，绝不入库） =====
scripts/keys/
scripts/*.keys.json
scripts/insert-keys*
*batch-insert-providers.cjs
*batch-insert-models.cjs
api-token.tmp

# ===== 临时测试文件（API 冒烟、生成测试、备份） =====
# 以 _ 开头的根目录文件通常是测试残留，不入库
/_*.json
/_*.txt
/_*.md
*_backup_*.json
*_result.json
*_smoke_*.json

# ===== 本地终端输出捕获（IDE/PowerShell 跑服务时的 stderr 重定向） =====
backend3001.err
vite3099.err
tmp_clean.txt
