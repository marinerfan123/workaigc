# 02 · 全模块职责说明（含跨文件调用关系）

> 本文按「模块 → 作用 → 对外导出 → 调用了谁 → 被谁调用 → 关键状态 → 设计依据 → 重构备注」逐一拆解。
> 重点不是罗列代码，而是**打通模块之间的调用关系**与**状态如何跨模块流动**。
> 🟥 = 业务规则（不可改）· 🟩 = 旧技术实现（可换）。源码行号来自本会话实读。

---

## A. 接入层

### A1 server/server.js（Node 原生 HTTP，无框架）
- **作用**：单进程同时托管 SPA(dist) + 全部 API（3001）。路由分发、限流、幂等查重、SNAKE_MAP 契约、initDB、SSE 出口。
- **对外关键接口**：`/api/generate`(POST, L2374)、`/api/generate/cancel/:taskId`(L2506)、`/api/generate/status/:id`(L2517)、`/api/generate/active`(L2525)、`/api/generate/stream`(SSE, L2535)、`/api/credits/webhook/:type`（支付回调用）。
- **调用了谁**：`dispatcher.*`（generateAsync/cancelTask/getTaskStatus/listActiveTasks）、`billing.*`、`accounting.*`、`realtime.*`、`auth`、`shop`、`admin`、`finance`、`payments`、`reference-styles`、`seed-easypay`。
- **被谁调用**：前端 `api.ts` 经 `apiFetch` 调用；SSE 由 `EventSource('/api/generate/stream')` 建立。
- **关键状态（内存）**：`SNAKE_MAP`(L780)、`fromSnake/toSnake`(L808/816)、`pgPool`（全局，`initDB` 失败 exit(1)）、`realUser`（cookie 会话解析）。
- **设计依据**：PG 唯一源铁律——`initDB` 连 PG 失败重试 5 次后 `exit(1)`（铁律·PG）。SNAKE_MAP 是 PG snake_case 与前端 camelCase 的唯一契约，**新增 DB 列必须同步**（铁律·SNAKE_MAP，出错点 B）。
- **重构备注**：🟩 无框架原生 HTTP 是可替换的，但**路由契约、SNAKE_MAP、幂等逻辑是🟥业务规则**，换框架时必须原样保留。

### A2 src/services/api.ts（统一前端 API 客户端）
- **作用**：所有后端调用唯一出口，JWT Bearer / cookie 会话鉴权，统一错误解析（从 `API 4xx: {json}` 提取 `code`：NEED_RECHARGE/INSUFFICIENT）。
- **关键导出**：`apiGenerate`(L442)、`apiGetGenerationStatus`(L472)、`apiCancelGeneration`(L495)、`apiGetQueueStatus`(L515)、`apiGetProviders`(L101)、`apiGetModels`(L120)、`ensureApi`(L41)。
- **调用了谁**：`apiFetch`（内部 fetch 封装）。**被谁调用**：GenerationBar、useGenerationStream、各页面。
- **设计依据**：前端只认统一语义参数（ratio/resolution/quality/count/duration/videoMode/prompt/negative/reference），不感知 provider 字段（铁律·配置主流，第 11 章）。
- **重构备注**：🟩 可用 axios/fetch 包装替换；🟥 `idempotencyKey` 必需、错误 `code` 透传语义必须保留。

---

## B. 编排层

### B1 server/dispatcher.cjs（调度核心，已实读全文）
- **作用**：异步生成编排。负责并发信号量、多供应商均衡、账号令牌桶、视频 submit+poll、等待区、看门狗、崩溃恢复、取消信号、终态结算。
- **对外导出**（L1081）：`generate, generateAsync, getTaskStatus, listActiveTasks, resumeRunningTasks, resumeWaitingArea, persistProviderTaskId, finalizeResumedTask, genericVideoPoll, startStuckTaskWatchdog, getWaitingAreaStatus, enqueueWaiting/dequeueWaiting, waitingAreaTriggered, runWaitingPump, updateTaskStatus, planPriority, cancelTask, setLogSink, logError` 等。
- **调用了谁**：`billing.*`(reserve/commit/release)、`accounting.recordConsumption`、`providers/video/index.cjs`(submit/poll)、`providers/video/shared.cjs`(pollLoop)、`realtime.emitTaskUpdate`。
- **被谁调用**：server.js（generate 路由、cancel 路由、stream 路由）、server.js 启动流程（`resumeRunningTasks` + `resumeWaitingArea` + `startStuckTaskWatchdog`）。
- **关键状态（内存）**：
  - `GLOBAL_ACTIVE/GLOBAL_MAX`(L26) 全局并发信号量；`RR_POINTER`(L28) 轮询指针。
  - `cancelledTasks`(L33) 取消信号 Set（进程重启清空，已落库任务由崩溃恢复跳过）。
  - `ACCT`(L38) 每账号令牌桶（单实例内存态，多实例需迁 Redis）。
  - `WAITING_AREA`(L1106) 等待区 Map；`WAITING_THRESHOLD=10`(L1107)；`WAITING_MAX_WAIT_MS=90min`(L1109)。
  - `PLAN_PRIORITY`(L1102) 会员优先调度。
- **设计依据**：
  - 多供应商均衡：同 model_id 下所有 enabled+api_key 合法 provider 进 pairs，RR 分配，拒单静默切下一个（dispatchOne L527）。
  - 账号令牌桶：B 单位/60s 回流，成本按档位（1k/2k/4k/video）扣，manual_state 热改生效（getAcct L392）。
  - 终态优先级：成功 > timeout(保留) > throttled(等待区) > failed（generate L634）。
- **重构备注**：🟩 内存态 `ACCT/WAITING_AREA/cancelledTasks` 在多实例横向扩展时需迁 Redis（deployment-plan §6）；🟥 均衡策略、令牌桶语义、终态优先级、取消护栏是业务规则。

### B2 server/providers/video/index.cjs（视频适配器路由）
- **作用**：把 (provider,model) 解析到具体适配器（agnes/minimax/volcano/generic）。
- **关键函数**：`resolveKey`(L13，显式>base_url 正则>generic)、`submit`(L34)、`poll`(L43)、`submitAndPoll`(L25)。
- **调用了谁**：`agnes.cjs/minimax.cjs/volcano.cjs`；**被谁调用**：dispatcher.videoGenerate(L215-225)。
- **设计依据**：submit/poll 拆分使「提交即持久化 providerTaskId」成为崩溃恢复地基（shared.cjs pollLoop 复用）。

### B3 server/providers/video/shared.cjs（共享视频工具，已实读）
- **作用**：通用 HTTP、JSONPath、状态归一、自适应轮询循环（pollLoop L187）。
- **关键**：`pollLoop` 防僵尸安全线 90min，超时返 `timeout` 非 `error`（L222）；`normalizeVideoStatus` 把供应商状态归一到 success/failed/pending（L175）；`buildVideoContent` 多模态 content 构造（L146，MiniMax/Volcano 字段差异）。
- **重构备注**：🟩 可替换为统一 SDK；🟥 90min 安全线 + 超时返 timeout + failed 终态化是业务规则。

---

## C. 账务层

### C1 server/billing.cjs（双余额三段式，已实读）
- **作用**：reward/recharge 双池计费的 reserve/commit/release，原子扣减、幂等。
- **关键函数**：`resolvePayment`(L11，奖励优先→回退充值→都不够抛错)、`reserveCredits`(L32)、`commitCredits`(L58，幂等)、`releaseCredits`(L70，幂等)、`findDanglingReserves`(L84，对账兜底)。
- **调用了谁**：`pgPool`（users/reward_credits/recharge_credits/credit_transactions）；**被谁调用**：server.js 生成路由、dispatcher 终态处理、cancelTask。
- **设计依据**：`users.credits` 为 STORED 列 = reward+recharge。`WHERE 池>=amount` 一行原子扣，无需事务。幂等靠 `credit_transactions.ref+kind` 唯一（_hasPosted L48）。
- **重构备注**：🟥 双池优先序、三段式时序、幂等键是业务规则，必须原样；🟩 表结构/存储过程可换，语义不能变。

### C2 server/accounting.cjs（全局双边记账）
- **作用**：每笔真实消耗双边记账——后台量（model_cost_rates）× 客户量（settings.app.creditToCents 折算），差值=盈亏。
- **关键函数**：`recordConsumption`(L55，幂等 + try/catch 不阻断主链路)；**被调用点**：dispatcher 成功分支（generateAsync.then L695 / resume L927 / waiting pump L1289）。
- **设计依据**：全局所有模型消耗（含系统自身）都走账、无例外、精确算量（铁律·账务，第 3 章）。系统调用 scope='system'、客户收费传 0。
- **重构备注**：🟥 强制埋点（generate/skill:*/agent:optimize-prompt）、双边算量、幂等是业务规则。

---

## D. 实时层

### D1 server/realtime.cjs（SSE 基础设施，已实读）
- **作用**：EventEmitter 按 userId 隔离的 SSE 通道；emitTaskUpdate 推终态；subscribe 注册连接；snapshotActive 回灌在途快照。
- **关键函数**：`emitTaskUpdate`(L20)、`subscribe`(L30)、`snapshotActive`(L53)；**被谁调用**：dispatcher 终态处理、server.js /api/generate/stream。
- **设计依据**：纯内存单进程通道，不做跨进程持久化（持久化由 PG 负责）。前端另有轮询兜底，SSE 异常不影响完成判定（useGenerationStream L60）。
- **重构备注**：🟩 单进程 EventEmitter 在多实例需迁 Redis pub/sub；🟥 「按 userId 隔离、连接即回灌快照、字段形状对齐 getTaskStatus」是业务规则（防多用户串看 G1）。

---

## E. 持久 / 缓存 / 资产层

- **PG17**（db.cjs + server.js 自有 initDB）：唯一数据源。所有表变更进 server.js 的 initDB 重启即生效（铁律·PG）。
- **Redis7.2**：仅缓存，不参与计费/任务状态（🟩 可去掉）。
- **OSS 阿里云**：业务服务器零二进制，仅预签名；用户资产成功即传 OSS 落 `oss_url`（铁律·OSS，已落地）。前端直传后回写 media。

---

## F. 业务/管理模块（✅ 已全部逐行实读核验，2026-08-11）

> 四模块均为**工厂函数 + 闭包依赖注入**模式：模块仅导出 `createX(ctx)`，工厂返回的对象才是路由处理器；文档此前所列「关键导出」多为闭包内部函数，仅经路由间接触达。

### F1 payments.cjs（充值订单 + 支付通道，fail-closed）
- **导出**：`createPayments(ctx)`(L18) → 返回 `{createOrder, listOrders, getOrder, handleWebhook, handlePayments, invalidateProviderCache}`(L255-260)；`loadPaymentSettings`(L34) 为内部函数。
- **子目录 payments/**：loader.cjs（60s 缓存 + weight 加权随机选通道）、webhook.cjs（四关）、crypto.cjs（AES-256-GCM，`iv.tag.ct` 格式，PAYMENT_MASTER_KEY 缺失/非 32 字节即抛错）、provider.cjs（抽象基类）、providers/easypay.cjs（MD5 排序拼接验签）、order-expiry.cjs（60s 扫过期单，不碰余额）。
- **webhook 四关**（webhook.cjs:47-181）：①验签（失败记 payment_audit+400）②金额校验（不等→ROLLBACK+suspicious+400）③订单状态守卫（FOR UPDATE；已 paid→幂等 200）④webhook_events 唯一键幂等去重。前置第 0 关：无 PG→503、无通道→400。
- **入账**（单事务）：本金+bonus **均入 recharge_credits 池**，流水 kind='grant'/pool='recharge'，ref=订单id（bonus 为 `订单id:bonus`）。
- **fail-closed 点**：无 PG/无 settings/未 enabled/全局关 wxpay|alipay/无 provider→createOrder 一律 503；金额以后端 topup_packages 为准（防前端篡改 bonus）。
- **被调用**：server.js:1548（payment-methods 公开）、:1562（webhook 网关前，正则仅 `[a-z]+`）、:1710（orders 系列）；PAYMENT_MASTER_KEY 兼作订单 HMAC 签名密钥（可回退 PAYMENT_SIGN_SECRET）。
- ⚠️ 已核验瑕疵：`allow_test` 列存在但无人读；createOrder 的 INSERT 不写 provider_id/channel_method；入账流水 balance_after 取 `credits` 列而非被加的 `recharge_credits` 列。

### F2 finance.cjs（后台账务总控，❌ 非「供应商余额管理」）
- **实际作用**：账务总览/充值订单/对账/单用户账本/充值套餐 CRUD/支付全局设置/支付服务商管理。**无供应商余额管理逻辑**（文档此前误判）；不引用 consumption_ledger/model_cost_rates/creditToCents/accounting.cjs。
- **导出**：`createFinance(ctx)`(L15) → `{handleFinance(L398), handlePublic(L474), overview, listRecharges, reconcile, userLedger, listPackages, ...}`(L482-486)。
- **reconcile 快照对账**（L125-159）：reserve 减/release 加模拟，commit/grant/adjust 以 `balance_after` 快照为权威校准；sim≠users.credits 报警（无快照且余额≠50 才报）。
- **支付配置**：密钥入库前 AES-256-GCM 加密（payments/crypto.cjs），API 只返 has_* 布尔，留空保留原值；全变更写 payment_audit；类型白名单 easypay/alipay/wxpay/stripe/mock。
- **被调用**：server.js:1546（公开 topup-packages）、:1653（`/api/admin/finance/*` 优先于通用 admin 分发）；模块内 requireAdmin（role∈{admin,system}）。

### F3 shop.cjs（技能注册表 + 数字能力包市集）
- **导出**：仅 `createShop(ctx)`(L19) → `{handleShop}`(L407)；runSkill(L124)/acquireProduct(L227) 为闭包内部函数。
- **runSkill 链路**（L124-191）：查 skill_registry(enabled)→`reserveDual`（billing 双池，赠送优先）→adapter 分发（prompt_optimize/text_gen）→pickTextModel 三层选模型（settings.app.promptOptimizeModel → agent_providers 按 priority/weight → 最便宜 type=text）→**直接 fetch `{base_url}/chat/completions`**（❌ 不经 /api/generate，文档此前误判；仅计费与生成流同源）→成功 commit / 失败 release→`accounting.recordConsumption`（scope:'user'，purpose:`skill:{key}`）。
- **acquireProduct**（L227-257）：仅 published 可买；已拥有幂等短路不扣费；price_cents>0→402 cash_required（现金收银台未做）；price_credits 走 billing 三段式 ref=`acquire:{id}:{userId}`；INSERT user_skills ON CONFLICT DO NOTHING。
- **被调用**：server.js:1576-1582（公开 GET）、:1655-1657（admin skills）、:1705-1707（兜底）。⚠️ `/api/cart`、`/api/orders`、`/api/products/` 前缀在 shop.cjs 无对应路由（死分发）。

### F4 admin.cjs（运营总控台 + 智能体层后台）
- **导出**：仅 `createAdmin(ctx)`(L7) → `{handleAdmin(L591), streamConsole(L276), requireAdmin(L19)}`；❌ pushSamplesToUsers(L400) 是内部函数（仅经 POST /api/admin/samples/push 触达），文档此前误判为导出。
- **requireAdmin**：role∈{admin,system}；被 server.js 18 处内联路由复用。
- **示例分发双份实现**：`ensureUserDefaults` 在 **server.js:908-931**（❌ 不在 admin.cjs；注册:1334/登录:1358 触发，admin 不收）；`pushSamplesToUsers` 在 admin.cjs:400-427（手动推送，排除 admin）。两者幂等键均为 (user_id, default_key)，拷贝字段一致（is_default=TRUE, source='default', tags::jsonb, oss_*）。模板种子 DEFAULT_ASSET_SEED 6 条（server.js:885-892，启动:740）。
- **管理员种子**：❌ 不在 admin.cjs；在 server.js:715-726（无 admin 且设 ADMIN_SEED_PASSWORD 才建，opt-in；默认密码启动告警 :3657）。
- **积分调整**（recharge L45-73）：只动 recharge_credits 池、非零整数、扣减不为负、双写 credit_transactions(kind='adjust',ref=`admin:{actorId}`)+audit_logs。⚠️ 缺口：status/role/delete/samples/agents 等写操作**无 audit_logs 审计**（仅 recharge 有）。
- **streamConsole**（L276-321）：admin SSE，五事件 metrics/traffic/flow/log/agent，1s tick + 15s 心跳。

### F5 其余模块
| 模块 | 作用 | 要点 |
|---|---|---|
| reference-styles.cjs | 参考样式库 | `createReferenceStyles(ctx)` → `{handle(L245), handleAdmin(L262)}`；分成**真实转账**（已核验）：server.js `creditStyleDesigner`(L1272)，media upsert(L2012) 触发，进设计者 reward_credits + style_earnings 台账，media_id 幂等，仅 approved，自用不分成 |
| seed-easypay.cjs | 易支付种子 | scripts/ 下同名脚本用 crypto.cjs encrypt() 加密 EASYPAY_* UPSERT pp-easypay 行 |
| db.cjs | PG 连接池 + 基础 initDB | pool/redis/query/transaction 被各模块 require；其 initDB（6 基础表）仅 migrate.cjs 调用，server.js 不调（T5 已核验：两套 initDB 字段已漂移，重构须收敛） |

---

## G. 前端组件

### G1 src/components/GenerationBar.tsx（生成入口，已实读关键段）
- **作用**：构造 pending 占位（乐观 UI）→ 调 apiGenerate → 回写 taskId → 启动 pollTaskUntilDone。
- **关键函数**：`handleGenerate`(L994)、`checkBalance`(L980 双池前置校验)、`processResultImages`(L416，前端写 media+OSS)、`pollTaskUntilDone`(L506，调 waitForTask)、刷新恢复 useEffect(L1206，localStorage + /api/generate/active 双保险)。
- **状态传递**：`pendingIds`（前端占位 id）→ 随 apiGenerate 透传给后端 → 成功后 `onPendingAttachTaskId` 回写 taskId（L1136）→ 取消时 MediaCard 调取消端点。
- **设计依据**：余额不足**绝不走 mock 兜底**（L1121/L1187，不白送图）；401/402 优先弹登录/充值窗。

### G2 src/hooks/useGenerationStream.ts（SSE + 轮询兜底，已实读）
- **作用**：单例 EventSource 近实时收终态 + 每 3s 轮询兜底，timeoutMs 默认 95min（L61）。
- **关键**：`waitForTask`(L60)——SSE 与轮询任一命中 `done/failed/cancelled/not_found` 即 resolve；超时不误判失败（L93，成败只听生成端）。

### G3 src/components/ui/image.tsx（破图兜底）
- **作用**：`<img>` 统一封装，onError 兜底。**出错点 G**：errored 分支须随 `src` 重置（加 `useEffect(()=>setErrored(false),[src])`，2026-08-11 修复），否则破图不恢复。

---

## H. 综合：约定 / 出错点 → 模块 映射

| 约定/出错点 | 关联模块 | 说明 |
|---|---|---|
| 🟥 PG 唯一源 | server.js initDB | 失败 exit(1) |
| 🟥 SNAKE_MAP | server.js SNAKE_MAP | 漏列→前端 undefined 假死（出错点 B） |
| 🟥 OSS 零二进制 | 前端直传 + OSS | 成功即落 oss_url |
| 🟥 超时以生成端为准 | dispatcher + shared.pollLoop | 90min 安全线，超时返 timeout（出错点 C,#578） |
| 🟥 双余额三段式 | billing.cjs + server.js 路由 | reserve/commit/release 时序 |
| 🟥 全局双边记账 | accounting.cjs | 强制埋点、无例外 |
| 🟥 支付 fail-closed | payments.cjs + seed-easypay | webhook 四关、AES-256-GCM |
| 出错点 A 打不开先二分 | 全部（进程存活） | agent 后台进程不跨会话，stable 用 `npm run dev` |
| 出错点 D 双积分 | SNAKE_MAP + 前端徽章 | supports_reward_balance/reward_credits_required 必须同步 |
| 出错点 E 删写库后刷新 | 前端各列表 | 真调 API + 主动刷新 |
| 出错点 F 不假百分比 | GenerationBar | indeterminate 扫光 + 真实已等待时间 |
| 出错点 G 破图 | ui/image.tsx | errored 随 src 重置 |
| 出错点 H 崩溃续轮询 | dispatcher.resumeRunningTasks | provider_task_id 落库续轮询 |
| 出错点 I 取消 | dispatcher.cancelTask + server.js cancel 路由 | 释放 held + 标 canceled + SSE |
| 出错点 J 视频 failed 透传+看门狗 | dispatcher.attemptOnAccount + startStuckTaskWatchdog | failed 立即终态(#602)；>3h 孤儿回收(#603) |
| 出错点 K 等待区崩溃恢复 | dispatcher.resumeWaitingArea + persistWaitingOpts | 内存队列持久化(#604) |

> 下一步读 **03_业务规则&异常处理规范**（🟥 业务规则逐条 + 🟩 技术实现 + 出错点详述）。
