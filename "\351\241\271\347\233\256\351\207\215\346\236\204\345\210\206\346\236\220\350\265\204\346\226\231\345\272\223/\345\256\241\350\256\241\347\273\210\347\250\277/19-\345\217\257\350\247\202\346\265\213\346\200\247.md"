# 19 · 可观测性（重构前系统体检）

> 范围：墨灵 AI 后端 `server/` 日志/监控/审计能力 + 前端 `src/` 错误监控
> 方法：只读源码审计（logbus / monitor / syslog / realtime / auth / accounting / 前端 hooks）
> 铁律遵循：未修改 `E:\code` 下任何源码/配置/数据库；结论以源码为准；无法实测者标【未知·待核验】
> 证据等级：`源码已证实` / `运行时已证实` / `推断` / `未知·待核验`

---

## 1. 现状盘点

| 能力 | 现状 | 源码位置 | 证据等级 |
|---|---|---|---|
| 结构化日志（格式） | 有。logbus 统一采集：`{id,ts,level(INFO/WARN/ERROR),source,message,meta}`，环形缓冲 1000 条；自动 hook `console.warn/error`（带 5s 去重）。但**落库前为内存流，非集中式日志系统（无 ELK/Loki/OTLP）** | `server/logbus.cjs:16-52,117-145` | 源码已证实 |
| 错误上报 / 持久化 | 有。所有 `ERROR`（含 console.error 自动捕获 + 业务 `logbus.emit('ERROR')`）经 `persistError` 落 `system_error_logs` 表（fire-and-forget，含 meta/stack）。进程级 `uncaughtException`/`unhandledRejection` 兜底落库 | `server/syslog.cjs:35-53,116-136`、`server/server.js:68-72` | 源码已证实 |
| 错误查询/告警 | 有查询无告警。`syslog.queryErrors` 提供后台历史错误列表（按 category/keyword/时间过滤 + 统计），由 `/api/admin/errors` 复用；**无主动告警（邮件/Webhook/钉钉）、无阈值触发** | `server/syslog.cjs:56-113` | 源码已证实 |
| metrics / 监控端点 | 有进程内指标，无标准端点。monitor.cjs 维护 500 条请求环形缓冲 + 60s 滚动窗口指标（QPS/成功率/P95/错误/总请求），经 SSE 推后台实时监控页；**无 `/metrics`（Prometheus）端点，无跨实例聚合，进程重启清零** | `server/monitor.cjs:14-69,125-133` | 源码已证实 |
| 健康检查端点 | 有。`GET /api/healthz` 返回 `{status,pg,redis,uptime,version,ts}`（公开，供探针/压测）。**单点、无就绪/存活分离（readiness/liveness）** | `server/server.js:1580-1589` | 源码已证实 |
| request id / 链路追踪 | **无**。`server.js` 全仓检索无 `x-request-id` / `correlationId` / `reqId` 生成与透传；一次请求跨模块无法串联 | 全仓检索无匹配 | 源码已证实 |
| 审计日志（谁在何时做了什么） | 部分有。**`system_error_logs` + `consumption_ledger`(actor_id) + `reference_styles` 审核 + `style_earnings`** 具备部分轨迹；但**无统一审计表覆盖「用户操作（增删改素材/模型/provider/-admin 动作）」**，`server.js:421` 附近的 `audit` 表定义需核实是否启用 | `server/server.js:421,657,674`、`server/accounting.cjs:55-110` | 源码已证实（部分） |
| 审计 actorId 失真（已知问题） | **存在**。`appGateway` 在 `devTokenEnabled` 时优先把 `Authorization: Bearer <API_TOKEN>` 解析为 `{id:'__system__',role:'system'}`。任何携带该 dev token 的请求都会被记成 `__system__`，使 `consumption_ledger.actor_id`、审计轨迹**不能反映真实用户**，且 dev token 非生产亦可能被复用 | `server/server.js:1274-1283,2534,2763,2908` | 源码已证实 |
| 前端错误监控（Sentry 等） | **无**。`src/` 全仓检索无 Sentry / `@sentry` / `window.onerror` 全局上报；仅各页面 `EventSource.onerror` 用于 SSE 断线重连状态，无错误上报到后端/第三方 | `src/` 全仓检索无匹配 | 源码已证实 |
| 集中日志 / 日志级别路由 / 采样 | **无**。日志仅在进程内存环形缓冲 + 错误落库；INFO/业务日志不落库、不转发；无采样、无结构化导出 | `server/logbus.cjs` | 源码已证实 |
| 前端→后端 tracing 透传 | **无**。前端 SSE 流（logbus/monitor/realtime）仅订阅，不携带/回传 request id；无法把一次用户操作关联到后端日志 | `server/realtime.cjs`、`src/hooks/*` | 源码已证实 |
| 业务关键事件埋点（生成提交/完成/计费/支付） | 部分。`accounting.recordConsumption`（双边账务，幂等）、billing 流水、`/api/admin/logs` 错误流存在；但**生成任务的「提交→调度→完成/失败」关键状态变更未统一进审计/事件总线外的可检索轨迹**（仅 PG 状态 + SSE 通知） | `server/accounting.cjs`、`server/dispatcher.cjs` | 源码已证实 |

---

## 2. 缺失项清单（P0 / P1）

### P0（可观测性基线，缺失即无法运维）
1. **无 request id / 分布式追踪** — 全链路无 `x-request-id` 生成与透传，SSE/DB/provider 调用无法串联；多实例或高并发排障几乎不可能。证据：`server.js` 全仓无匹配。证据等级：源码已证实。置信度：高。
2. **审计 actorId 失真** — `appGateway` 的 dev token Bearer 优先把请求记成 `__system__`，导致 `consumption_ledger.actor_id` 与任何基于它的审计轨迹失真，无法追责到真实用户。证据：`server/server.js:1274-1283`。证据等级：源码已证实。置信度：高。
3. **无统一操作审计轨迹** — 仅错误日志与账务表具备部分轨迹；用户/管理员的关键写操作（增删改素材、模型、provider、额度调拨、风格审核）**无统一审计表记录「谁/何时/做了什么/前后值」**。证据：`server.js` 路由层无统一 audit 包裹。证据等级：源码已证实。置信度：中。
4. **前端错误监控缺失** — 无 Sentry/全局 `window.onerror` 上报，前端运行时异常（白屏、组件崩溃、SSE 静默断线）后端与团队完全无感知。证据：`src/` 全仓无匹配。证据等级：源码已证实。置信度：高。

### P1（补齐可观测性能力）
5. **无集中式日志** — 日志为进程内存环形缓冲 + 仅错误落库；无 ELK/Loki/OTLP 转发，多实例无法统一检索，重启即丢 INFO/WARN。证据：`server/logbus.cjs`。证据等级：源码已证实。置信度：高。
6. **无标准 metrics 端点 / 无告警** — monitor.cjs 仅内存 SSE 指标，无 `/metrics`（Prometheus）；`syslog` 有查询无主动告警（邮件/Webhook/钉钉）。无法设 SLO、无法在错误率/QPS/延迟异常时触发通知。证据：`server/monitor.cjs`、`server/syslog.cjs`。证据等级：源码已证实。置信度：高。
7. **健康检查过于单薄** — 仅 `/api/healthz` 单点，未分离 readiness/liveness，未覆盖依赖真实健康（如 Redis 降级态、PG 连接池耗尽、等待区积压）。证据：`server/server.js:1580-1589`。证据等级：源码已证实。置信度：中。
8. **双 Redis 语义不透明** — `db.cjs` 自带无降级 Redis 实例，监控/限流状态来源不统一，故障难定位。证据：`server/db.cjs:23-33`。证据等级：源码已证实。置信度：中。
9. **指标/日志进程内存态，不可跨实例聚合** — 单实例约束下尚可，但与 P0-1 的扩展需求矛盾；一旦放开多实例，现有监控即失效。证据：`server/monitor.cjs:14`、`server/logbus.cjs:24`。证据等级：源码已证实。置信度：高。

### 待核验（本环境无法实测）
- 生产环境实际日志量 / 错误率 / P95 延迟曲线 — 【未知·待核验】
- `system_error_logs`、`consumption_ledger` 表是否已在运行实例中积累数据 — 【未知·待核验】
- 是否已有外部 APM（如服务器层 Prometheus Node Exporter / 容器探针）— 【未知·待核验】

---

## 3. 重构可观测性建议（目标态）

> 原则：与「P0 单实例锁」解耦——可观测性层应先行云原生化，为后续多实例铺路。

1. **统一 request id + 分布式追踪**
   - 每个入站请求生成 `x-request-id`，贯穿 HTTP → DB 查询 → provider 调用 → SSE 事件；接入 OpenTelemetry（OTLP），Span 覆盖生成任务「提交→调度→轮询→完成」。
   - 前端 SSE/API 客户端回传并在日志中带入 request id，实现端到端串联。
2. **修复审计 actorId**
   - `appGateway` 废除「dev token Bearer 优先解析为 `__system__`」逻辑；系统身份仅限内部服务间调用（mTLS/独立 `x-system-key`），用户请求必须以会话 cookie 的 `realUser.id` 为准；`consumption_ledger.actor_id` 一律取真实用户。
3. **统一审计表 + 中间件**
   - 新增 `audit_trail(action, actor_id, target_type, target_id, before, after, ts, request_id)`，对所有写操作（素材/模型/provider/admin 调额/风格审核）用统一中间件包裹，闭合「谁在何时做了什么、改了什么」。
4. **集中式日志**
   - 日志经 OTLP/JSON 转发至 Loki/ELK；保留 level/source/request_id/meta 结构化字段；按环境采样；INFO 不落库但全量进集中存储。
5. **标准 metrics + 告警**
   - 暴露 `/metrics`（Prometheus）：HTTP QPS/延迟分位、PG 连接池占用、Redis 命中/降级、生成任务吞吐/等待区积压/失败率、计费流水速率、SSE 连接数。
   - 接 Alertmanager：错误率阈值、P95 超界、PG 池耗尽、Redis 降级、等待区持续积压、进程重启 触发邮件/Webhook/钉钉。
6. **健康检查分级**
   - `liveness`（进程存活）与 `readiness`（PG 可达 + 池有余量 + Redis 可用 + 等待区未爆）分离；探针带依赖真实探测。
7. **前端错误监控**
   - 接入 Sentry（或等价），捕获未处理异常/console.error/资源加载失败/SSE 断线，带 `user_id`（脱敏）+ `request_id` 上下文上报。
8. **监控/日志外部化、去进程内存态**
   - monitor/logbus 指标与日志改为推送到外部采集器，进程重启不丢；为多实例聚合做准备（与 dispatcher 状态迁 Redis 同步推进）。

---

## 4. 证据等级与置信度汇总

| 条目 | 证据等级 | 置信度 |
|---|---|---|
| 结构化日志（logbus 内存 + 错误落库） | 源码已证实 | 高 |
| 错误落库 system_error_logs + 进程级兜底 | 源码已证实 | 高 |
| 有查询无告警（syslog.queryErrors） | 源码已证实 | 高 |
| 进程内指标 monitor.cjs（无 /metrics） | 源码已证实 | 高 |
| /api/healthz 单点健康检查 | 源码已证实 | 高 |
| 无 request id / 分布式追踪 | 源码已证实 | 高 |
| 审计 actorId 失真（dev token 优先） | 源码已证实 | 高 |
| 无统一操作审计轨迹 | 源码已证实 | 中 |
| 前端无 Sentry/错误监控 | 源码已证实 | 高 |
| 无集中日志 / 无告警 | 源码已证实 | 高 |
| 生产实际指标/日志量 | 未知·待核验 | — |

---

## 5. 摘要

墨灵 AI 当前具备**基础但非云原生的可观测性**：错误经 logbus 落 `system_error_logs` 并可后台查询，monitor.cjs 提供进程内请求指标与实时监控 SSE，健康检查 `/api/healthz` 存在。但缺四大关键能力：

- **无 request id / 分布式追踪**（全链路不可串联，P0）；
- **审计 actorId 失真**——`appGateway` 把 dev token Bearer 优先解析为 `__system__`，`consumption_ledger.actor_id` 与审计轨迹无法反映真实用户（P0，已知问题）；
- **无统一操作审计轨迹**（用户/管理员写操作无「谁/何时/改了什么」闭环，P0）；
- **前端无错误监控**（Sentry/全局 onerror 缺失，P0）。

P1 缺口为：无集中日志（仅进程内存+错误落库）、无标准 `/metrics` 与告警、健康检查未分级、双 Redis 语义不透明、监控指标进程内存态不可跨实例聚合（与单实例锁耦合）。

重构建议以「可观测性先行云原生化」为原则：统一 request id + OTLP 追踪、修复 actorId、补统一审计表、接集中日志与 Prometheus/告警、分级健康检查、前端接 Sentry，并将监控/日志去进程内存态，为多实例扩展铺路。生产实际指标曲线与日志量因本环境无法实测，标【未知·待核验】。
