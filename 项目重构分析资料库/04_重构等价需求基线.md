# 04 · 重构等价需求基线（验收清单）

> 用途：重构后逐功能核对「行为 100% 对齐」。每份条目给：
> **① 必须保留的业务行为**（跨文件调用链 + 源码锚点）→ **② 可替换技术实现** → **③ 验收口径**。
> 🟥 = 行为不可偏差；🟩 = 实现手段可换。

---

## F1 提交生成请求
- ① 前端 `handleGenerate` 构造 pending 占位 → `apiGenerate`(POST /api/generate) → 后端限流(30/60s) + 登录校验 + 幂等查重 + 返回 `{status:'pending', taskId}`。失败（401/402/缺参）即时前端提示，**不建幽灵占位、不走 mock 兜底**（GenerationBar.tsx:1019/1120/1186）。
- ② 路由框架、限流中间件、UUID 方案可换。
- ③ 同参数连点两次（同 idempotencyKey）只扣一次费；余额不足弹充值窗而非出图；网络 402 不误判为生成失败。

## F2 计费扣减（双余额三段式）
- ① reserve(提交即扣,服务器.js:2431) → commit(成功,dispatcher.cjs:688) / release(失败/取消/异常,dispatcher.cjs:731/755/1240)。奖励优先→回退充值→都不够抛错（billing.cjs:11）。幂等靠 `credit_transactions.ref+kind`（billing.cjs:48）。
- ② 存储过程/表结构可换。
- ③ 成功仅 commit 一笔；失败/取消/超时(waiting) 积分仍 held 不释放；重复 ref 不双扣；双池优先序与现网一致。

## F3 异步调度 + 多供应商均衡
- ① `generateAsync` INSERT running → `generate` 解析 models→providers 组装 pairs → `dispatchOne` RR 均衡、拒单静默切下一个（dispatcher.cjs:527-590）。全局信号量 GLOBAL_MAX(默认10) 可从 settings.app.maxThreads 实时覆盖（dispatcher.cjs:555）。
- ② 信号量/均衡算法实现可换；内存态可迁 Redis。
- ③ 同 model_id 多 provider 时流量被均衡分摊；单账号冷却/桶空/并发满时切下一个而不报错；全局并发上限生效。

## F4 账号令牌桶限流
- ① 每账号 B 单位/60s 回流，按档位(1k/2k/4k/video)扣；manual_state 热改生效（dispatcher.cjs:392-418）。瞬时错误(isTransient)有界重试吸收抖动（dispatcher.cjs:436/466）。
- ② 令牌桶可迁 Redis；成本公式可调。
- ③ 真 429 退还+冷却+拒单不扣费；确定性错误(无图片/鉴权失败)不重试；manual cold/hot 即时生效。

## F5 视频异步提交 + 续轮询
- ① 非 generic 走 `providers/video/*` submit→onSubmitted 持久化 providerTaskId→poll；generic 走内联 submit+poll（deadline 90min）（dispatcher.cjs:214-301）。状态归一 success/failed/pending（shared.cjs:175）。
- ② 适配器可统一 SDK；轮询库可换。
- ③ 提交成功即落 provider_task_id；生成端 success 拿回 URL、failed 立即终态、pending 续等；90min 安全线超时返 timeout 不判失败。

## F6 崩溃恢复
- ① 视频 provider_task_id + resume_meta.submittedAt 落库（dispatcher.cjs:776）；启动 `resumeRunningTasks`(L795) 续轮询 → `finalizeResumedTask`(L907) 与正常完成同逻辑；`resumeWaitingArea`(L822) 重入队等待区孤儿。
- ② 扫描 SQL 窗口(6h/3h)可调；可加 leader 选举避免多实例重复 resume。
- ③ 模拟 kill 进程后重启，在途视频任务最终 done/failed（非永久 running）；积分不泄漏（held 最终 commit 或 release）。

## F7 等待区 + 会员优先
- ① 全供应商不可用→throttled→enqueueWaiting + persistWaitingOpts + runWaitingPump 后台重试（dispatcher.cjs:709-718/1257）。会员(pro/team)出队优先（PLAN_PRIORITY L1102）。超阈值(10)且全资源 down→前台提示「资源不足」（waitingAreaTriggered L1175）。
- ② 内存 Map 可迁 Redis；阈值可调。
- ③ 资源恢复后会员任务先出队；等待中任务保持 running 不误判失败；超 WAITING_MAX_WAIT_MS(90min) 标 waiting 保留不退费。

## F8 取消任务
- ① `POST /api/generate/cancel/:taskId` → owner 校验→cancelledTasks→出等待区→释放 held→标 canceled→SSE（server.js:2506, dispatcher.cancelTask L1216）。generateAsync.then/finalizeResumedTask 护栏覆盖图片路径（L674/910）。
- ② 取消信号载体可换 Redis；终态码语义保留。
- ③ 取消后积分即时回退；pending 卡片消失；done/failed/canceled 取消返回 409；越权 403；owner 校验生效。

## F9 SSE 实时推送 + 轮询兜底
- ① `emitTaskUpdate` 推终态→SSE `/api/generate/stream`（server.js:2535）→前端 `waitForTask` 同时监听 SSE + 每 3s 轮询（useGenerationStream.ts:60），任一命中即 resolve；连接即回灌快照（realtime.snapshotActive L53）。
- ② 单进程 EventEmitter 可迁 Redis pub/sub；轮询间隔可配。
- ③ SSE 断开时轮询仍能拿到终态；刷新后快照回灌不漏事件；超时(95min)不误判失败。

## F10 刷新/挂载恢复
- ① localStorage（主）+ `/api/generate/active`（兜）双路恢复 pending 任务（GenerationBar.tsx:1206）；只恢复 running，不覆盖已落库好图。
- ② localStorage 可换 IndexedDB/服务端 session。
- ③ 刷新页面后在进行中的任务继续推进并最终落图；已完成的不会被 pending 占位覆盖。

## F11 参考样式归因
- ① 归因样式 previewUrl 实际进入本次生成 referenceImages（GenerationBar.tsx:1006）；生成结果回写 referenceStyleId（processResultImages ctx）。
- ② 归因 UI 可重做。
- ③ 选中归因样式后真图带该样式参考；未选则不带入。

## F12 双余额 + 三元徽章
- ① 奖励/充值优先序（A2）；前端三元徽章展示；后台填双积分须 SNAKE_MAP 含 supports_reward_balance/reward_credits_required（D）。
- ② 徽章 UI 可重做。
- ③ 余额扣减顺序与现网一致；双积分模型正确展示与计费。

## F13 支付（易支付 fail-closed）
- ① 无 provider→createOrder 503；loadPaymentSettings 异常→null→503；webhook 四关 fails-closed；密钥 AES-256-GCM（payments.cjs / seed-easypay.cjs）。
- ② 支付渠道/加密库可换。
- ③ 未配置支付时下单必 503；webhook 异常绝不静默成功；回调校验四关全过才入账。

## F14 全局双边记账
- ① 所有消耗走 accounting.recordConsumption，双边算量、幂等、try/catch 不阻断（accounting.cjs:55）。generate/skill:*/agent:optimize-prompt 强制埋点（A11）。
- ② 记账存储可换。
- ③ 每笔生成（含系统调用）均有双边记录；客户量/后台量差值=盈亏；重复 ref 不双记。

## F15 OSS 资产落地
- ① 业务服务器零二进制；用户资产成功即传 OSS 落 oss_url（A12）。
- ② OSS 厂商/SDK 可换。
- ③ 生成成功资产存 OSS 可长期访问；不外存裸第三方临时 URL（尽快转 OSS）；data URI 不落库。

## F16 管理后台 / 财务 / 市集 / 示例库
- ① admin.cjs(handleAdmin/streamConsole/pushSamplesToUsers)、finance.cjs(handleFinance，**非 admin.cjs**)、shop.cjs(runSkill/acquireProduct/handleShop)、reference-styles.cjs、seed-easypay.cjs 既有的路由契约与权限校验保留。
- ② 后台框架/UI 可重做。
- ③ 示例库分发（ensureUserDefaults/pushSamplesToUsers）仍把 default_assets 拷到个人 media(is_default/default_key/tags)；参考样式分成**真实转账**（已核验 2026-08-11）：server.js `creditStyleDesigner`(L1272) 按 commission_rate（默认 30%）进设计者 reward_credits + style_earnings 台账，media_id 幂等，仅 approved 样式，自用不分成——重构必须保留等价语义。

## F17 模型 / 供应商配置主流
- ① 前台只认公共语义（ratio/resolution/quality/count/duration/videoMode/prompt/negative/reference）；每模型 Capability Schema 声明档位+映射 provider 字段+输出解析路径（铁律·第 11 章）。新增 provider/model 尽量配置而非写死。
- ② 配置 schema 格式可演进（paramTemplate/mapping）。
- ③ 同业务新增模型零/低代码接入；provider.apiKey 永不出现在前端。

## F18 SNAKE_MAP 契约
- ① PG↔前端字段双向映射唯一来源（server.js:780-823）；新增列必同步（A9）。
- ② 换 ORM 须保留等价字段别名。
- ③ 任意新增 DB 列在前后端可见且类型一致，无 undefined 假死。

---

## 待核验清单（✅ 已全部核验完毕，2026-08-11）
- **T1 ❌ 确认不一致（行为缺口）**：MiniMax/Volcano 适配器在生成端明确 failed 时返 `error`（minimax.cjs:85 / volcano.cjs:121），而 generic 路径返 `failed`（dispatcher.cjs:289）。attemptOnAccount 仅对 `failed` 立即终态化（:497），`error` 被当瞬时错误切下一账号（:507）——**#602 修复在适配器层有缺口**。重构决策点：建议修复（适配器 failed 终态化）并补回归测试；若选择精确复刻须记录为已知缺陷。
- **T2 ✅ 真实转账**：`creditStyleDesigner`(server.js:1272) 将分成 UPDATE 进设计者 reward_credits（:1295）+ style_earnings 台账（:1297）；commission_rate 样式级优先、缺省 settings.app.styleCommissionRate（默认 30%）；media_id 幂等；仅 approved；自用不分成；异常不阻断。调用点 media upsert（:2012）。
- **T3 ✅ sellers 非真实实体**：全项目零匹配，无 sellers 表；设计者 = reference_styles.user_id / style_earnings.designer_id。
- **T4 ✅ 确认**：volcano 无独立 modelConfigs，归入 seedance.ts（volcano.cjs 即火山方舟 Seedance 适配器）。
- **T5 ✅ 确认分工**：db.cjs.initDB（6 基础表）仅 migrate.cjs 调用；server.js.initDB（L76，40+ 表，L3577 启动调用，失败 exit(1)）为运行权威。两套字段已漂移（models 表 mapping_name/credit_cost/supports_reward_balance/reward_credits_required 仅 server.js 有）——重构须收敛单一 schema 源。
- **T6 ✅ 确认**：modelConfigs 为 agent 草稿模块（schema.ts 注释「不依赖任何运行时逻辑」），src/ 无 import；运行时读 /api/models（DB models 表）。schema 字段见 05 文档 §6。

> 下一步读 **05_源码引用索引**（按 模块→文件→函数(行号)→职责→调用方/被调方 结构化）。
