# 05 · 源码引用索引

> 结构化索引：**模块 → 文件 → 函数(行号) → 职责 → 调用方 / 被调方**。
> 行号来自本会话实读（server.js / dispatcher.cjs / billing.cjs / realtime.cjs / providers/video/* / api.ts / GenerationBar.tsx / useGenerationStream.ts）。
> 未逐行实读的模块标 `【待核验：文件】`，重构前请 `read` 核实。

---

## 1. 接入层

### server/server.js
| 函数/符号 | 行号 | 职责 | 调用方 | 被调方 |
|---|---|---|---|---|
| `SNAKE_MAP` | 780 | PG↔camel 双向映射表 | — | `fromSnake`/`toSnake` |
| `fromSnake` | 808 | snake→camel | 各处查询返回 | SNAKE_MAP |
| `toSnake` | 816 | camel→snake | 各处写入 | SNAKE_MAP |
| `initDB` | (集中在文件前部) | PG 建表/迁移，失败重试5次 exit(1) | 启动 | pgPool |
| `/api/generate` POST | 2374 | 限流→登录→幂等→reserve→generateAsync | 前端 apiGenerate | billing.reserve/resolvePayment, dispatcher.generateAsync |
| `/api/generate/cancel/:id` | 2506 | 取消路由 | 前端 apiCancelGeneration | dispatcher.cancelTask |
| `/api/generate/status/:id` | 2517 | 查单任务 | 前端 apiGetGenerationStatus | dispatcher.getTaskStatus |
| `/api/generate/active` | 2525 | 列出在途(刷新恢复) | 前端恢复 | dispatcher.listActiveTasks |
| `/api/generate/stream` | 2535 | SSE 出口 | EventSource | realtime.subscribe/snapshotActive |

### src/services/api.ts
| 函数 | 行号 | 职责 | 调用方 |
|---|---|---|---|
| `apiGenerate` | 442 | POST /api/generate，解析 402 code | GenerationBar |
| `apiGetGenerationStatus` | 472 | 查状态(轮询兜底) | useGenerationStream |
| `apiCancelGeneration` | 495 | 取消 | MediaCard/GenerationBar |
| `apiGetQueueStatus` | 515 | 等待区聚合 | 前台提示 |
| `apiGetProviders` | 101 | 供应商列表 | 模型选择 |
| `apiGetModels` | 120 | 模型列表 | 模型选择 |

---

## 2. 编排层

### server/dispatcher.cjs（本会话实读全文）
| 函数 | 行号 | 职责 | 调用方 | 被调方 |
|---|---|---|---|---|
| `generateAsync` | 650 | 插 running + 后台跑 + 终态结算 | server.js:2492 | generate, billing, accounting, realtime |
| `persistProviderTaskId` | 776 | 视频提交即落 provider_task_id | videoGenerate.onSubmitted | PG |
| `generate` | 547 | 解析 pairs + 并行 dispatchOne + 汇总终态 | generateAsync | dispatchOne |
| `dispatchOne` | 527 | RR 均衡 + 拒单切下一个 + 终态返回 | generate | attemptOnAccount |
| `attemptOnAccount` | 448 | 令牌桶 + 调用 image/video + timeout/failed 透传 | dispatchOne | imageGenerate/videoGenerate |
| `imageGenerate` | 129 | 图片生成(同步,60s超时,瞬时重试) | attemptOnAccount | callEndpoint |
| `videoGenerate` | 214 | 视频 submit+poll(90min安全线) | attemptOnAccount | providers/video/*, cancelledTasks |
| `genericVideoPoll` | 307 | 崩溃恢复续轮询(generic) | resumeOneTask | pollLoop |
| `resumeRunningTasks` | 795 | 启动扫在途视频续轮询 | server.js 启动 | resumeOneTask |
| `resumeOneTask` | 863 | 重建轮询(取 provider/model) | resumeRunningTasks | videoRouter.poll/genericVideoPoll |
| `finalizeResumedTask` | 907 | 续轮询终态(同 generateAsync 逻辑+取消护栏) | resumeOneTask | billing/accounting/realtime |
| `startStuckTaskWatchdog` | 1002 | 每10min扫 running>3h 回收 | server.js 启动 | billing.release, updateTaskStatus |
| `scanStuckTasks` | 981 | 孤儿回收(仅 running,不碰 waiting) | watchdog | — |
| `getTaskStatus` | 1012 | 查单任务 | server.js:2517 | PG |
| `listActiveTasks` | 1043 | 列出在途(按 userId 过滤) | server.js:2525 | PG |
| `cancelTask` | 1216 | 释放 held+出等待区+标 canceled+SSE | server.js:2511 | billing.release, realtime |
| `runWaitingPump` | 1257 | 等待区重试泵(会员优先) | generateAsync.then | generate, billing |
| `enqueueWaiting` | 1138 | 入等待区 | generateAsync.then | WAITING_AREA |
| `persistWaitingOpts` | 1154 | 持久化 opts 到 resume_meta | generateAsync.then | PG |
| `resumeWaitingArea` | 822 | 重启重入队等待区孤儿 | server.js 启动 | runWaitingPump |
| `updateTaskStatus` | 1199 | 统一写任务状态 | 多处 | PG |
| `getWaitingAreaStatus` | 1179 | 聚合等待区状态 | apiGetQueueStatus | allResourcesDown |
| `waitingAreaTriggered` | 1175 | 全 down 且积压>阈值 | 前台提示 | allResourcesDown |
| 全局态 | 26/33/38/1102/1106/1107/1109 | GLOBAL_MAX/cancelledTasks/ACCT/PLAN_PRIORITY/WAITING_AREA/THRESHOLD/MAX_WAIT | — | — |

### server/providers/video/index.cjs
| 函数 | 行号 | 职责 | 被调方 |
|---|---|---|---|
| `resolveKey` | 13 | (provider,model)→适配器 key | adapters |
| `submit` | 34 | 仅提交(返回 providerTaskId) | agnes/minimax/volcano |
| `poll` | 43 | 续轮询(传 isCancelled) | adapters |
| `submitAndPoll` | 25 | 兼容旧全量 | adapters |

### server/providers/video/shared.cjs
| 函数 | 行号 | 职责 |
|---|---|---|
| `pollLoop` | 187 | 自适应轮询(90min安全线,超时返 timeout, failed 终态) |
| `normalizeVideoStatus` | 175 | 供应商状态→success/failed/pending |
| `buildVideoContent` | 146 | MiniMax/Volcano 多模态 content 差异 |
| `agnesVideoSize` | 116 | Agnes 分辨率推导 |
| `callEndpoint`/`fetchJson` | 45/75 | 通用 HTTP(60s abort) |
| `fillTemplate`/`getByPath` | 14/23 | 占位符/JSONPath |

---

## 3. 账务 / 实时层

### server/billing.cjs（实读）
| 函数 | 行号 | 职责 |
|---|---|---|
| `resolvePayment` | 11 | 奖励优先→回退充值→抛错 |
| `reserveCredits` | 32 | 原子扣到池 |
| `commitCredits` | 58 | 成功结算(幂等) |
| `releaseCredits` | 70 | 失败/取消回退(幂等) |
| `_hasPosted` | 48 | 幂等判据(ref+kind) |
| `findDanglingReserves` | 84 | 对账兜底(running>30min) |

### server/accounting.cjs
| 函数 | 行号 | 职责 |
|---|---|---|
| `recordConsumption` | 55 | 双边记账(幂等+不阻断) |

### server/realtime.cjs（实读）
| 函数 | 行号 | 职责 |
|---|---|---|
| `emitTaskUpdate` | 20 | 推终态给该 user 所有连接 |
| `subscribe` | 30 | 注册 SSE 连接(按 userId 隔离) |
| `snapshotActive` | 53 | 连接即回灌在途快照 |

---

## 4. 前端

### src/components/GenerationBar.tsx（实读关键段）
| 函数 | 行号 | 职责 |
|---|---|---|
| `handleGenerate` | 994 | 双池校验→pending占位→apiGenerate→回写 taskId |
| `checkBalance` | 980 | 前端双池前置校验(优先赠送→回退→拦截) |
| `processResultImages` | 416 | 成功后写 media+OSS 直传 |
| `pollTaskUntilDone` | 506 | 调 waitForTask + processResultImages |
| 恢复 useEffect | 1206 | localStorage + /active 双路恢复 |

### src/hooks/useGenerationStream.ts（实读）
| 函数 | 行号 | 职责 |
|---|---|---|
| `waitForTask` | 60 | SSE + 3s 轮询兜底，命中终态 resolve；超时(95min)不误判 |

### src/components/ui/image.tsx
| 符号 | 职责 | 备注 |
|---|---|---|
| 默认导出 | `<img>` 封装 onError 兜底 | 出错点 G：errored 须随 src 重置 |

---

## 5. 业务/管理模块（✅ 已逐行实读核验，2026-08-11）

> 四模块均为工厂模式：模块只导出 `createX(ctx)`，下列内部函数经工厂返回对象/路由触达。

### payments.cjs（+ payments/ 子目录）
| 函数 | 行号 | 职责 | 调用方 |
|---|---|---|---|
| `createPayments` | 18 | 工厂 → {createOrder, listOrders, getOrder, handleWebhook, handlePayments, invalidateProviderCache}(255-260) | server.js:1209 |
| `loadPaymentSettings` | 34 | 读 payment_settings（无 PG/无行/异常→null） | createOrder/listPaymentMethods |
| `createOrder` | 59 | 下单：套餐权威取值→限额/日额/待付数校验→落 recharge_orders→provider.createOrder | server.js:1710 |
| `handleWebhook` | payments/webhook.cjs:47 | 四关：验签(61)→金额(101)→状态(87/110)→幂等(116)；入账本金+bonus 均入 recharge_credits(136-161) | server.js:1562（网关前，正则仅 `[a-z]+`） |
| `encrypt/decrypt` | payments/crypto.cjs:55 | AES-256-GCM，iv.tag.ct；PAYMENT_MASTER_KEY 缺失即抛错 | loader/seed/finance |
| `createOrderExpiryWorker` | payments/order-expiry.cjs:96 | 60s 扫过期 pending→expired，不碰余额 | server.js:1219 |
| ⚠️ 瑕疵 | — | allow_test 列无人读；createOrder 不写 provider_id；入账流水 balance_after 取 credits 列而非 recharge_credits 列 | — |

### finance.cjs（❌ 非供应商余额管理——实为后台账务总控）
| 函数 | 行号 | 职责 | 调用方 |
|---|---|---|---|
| `createFinance` | 15 | 工厂 → {handleFinance, handlePublic, overview, listRecharges, reconcile, userLedger, listPackages...}(482-486) | server.js:1191-1199 |
| `handleFinance` | 398 | /api/admin/finance/* 分发（入口 requireAdmin 403/无 PG 503） | server.js:1653（优先于通用 admin） |
| `handlePublic` | 474 | GET /api/finance/topup-packages（公开） | server.js:1546 |
| `reconcile` | 125 | 快照对账：reserve/release 增减 sim，commit/grant/adjust 以 balance_after 权威；余额≠50 才告警 | handleFinance |
| 支付服务商 CRUD | 313-396 | 密钥 AES 加密入库、API 只返 has_*、全变更写 payment_audit | handleFinance |

### shop.cjs
| 函数 | 行号 | 职责 | 调用方 |
|---|---|---|---|
| `createShop` | 19 | 工厂 → 仅 {handleShop}(407) | server.js:1182-1188 |
| `handleShop` | 320 | 路由分发（公开/登录/admin 三档） | server.js:1576-1582/1655-1657/1705-1707 |
| `runSkill` | 124 | 双池 reserve→adapter→pickTextModel→**直连 chat/completions（不经 /api/generate）**→commit/release→recordConsumption | handleShop:336 |
| `pickTextModel` | 61 | 三层：settings.promptOptimizeModel→agent_providers→最便宜 text 模型 | runSkill |
| `acquireProduct` | 227 | 已拥有幂等短路；cash_required 402；billing 三段式 ref=acquire:{id}:{userId} | handleShop:394 |
| ⚠️ 死分发 | — | /api/cart、/api/orders、/api/products/ 前缀无对应路由 | server.js:1705 |

### admin.cjs
| 函数 | 行号 | 职责 | 调用方 |
|---|---|---|---|
| `createAdmin` | 7 | 工厂 → {handleAdmin, streamConsole, requireAdmin}(748)；**无种子逻辑** | server.js:1159-1170 |
| `requireAdmin` | 19 | role∈{admin,system}；被 server.js 18 处内联路由复用 | server.js 多处 |
| `handleAdmin` | 591 | /api/admin/* 兜底分发 | server.js:1702 |
| `streamConsole` | 276 | admin SSE 五事件 metrics/traffic/flow/log/agent | server.js:1651 |
| `recharge` | 45 | 积分调整：只动 recharge_credits+双写审计 | handleAdmin |
| `pushSamplesToUsers` | 400 | 内部函数：一键推送示例（排除 admin，幂等键 (user_id,default_key)） | 仅 POST /api/admin/samples/push |
| ⚠️ 注意 | — | ensureUserDefaults 在 **server.js:908-931**（非本模块）；status/role/delete 等写操作无 audit_logs 审计 | — |

### reference-styles.cjs / seed-easypay.cjs / db.cjs
| 文件 | 关键导出 | 要点 |
|---|---|---|
| reference-styles.cjs | createReferenceStyles → {handle(245), handleAdmin(262)} | 分成转账在 server.js creditStyleDesigner(1272)（见 T2） |
| scripts/seed-easypay.cjs | — | encrypt() 加密 EASYPAY_* UPSERT pp-easypay |
| db.cjs | pool/redis/initDB/query/queryOne/queryAll/transaction | initDB 仅 migrate.cjs:4,9 调用（T5） |

---

## 6. 待核验清单（✅ 已全部核验完毕，2026-08-11）
- **T1 ❌ 不一致（行为缺口）**：generic 返 `failed`（dispatcher.cjs:289）→ attemptOnAccount:497 立即终态化；MiniMax/Volcano 返 `error`（minimax.cjs:85 / volcano.cjs:121）→ :507 当瞬时错误切账号空转。shared.cjs:216 注释的设计意图（failed/error 区分）未被两适配器遵循。
- **T2 ✅ 真实转账**：server.js `creditStyleDesigner`(L1272)：分成进 reward_credits（:1295）+ style_earnings 台账（:1297）；rate 样式级 commission_rate 优先、缺省 settings.app.styleCommissionRate（默认 30，:1262-1267）；media_id 幂等（:1277）；仅 approved；自用不分成；异常 catch 不阻断。调用点：media upsert（:2012）。
- **T3 ✅ sellers 非实体**：零匹配；设计者由 reference_styles.user_id / style_earnings.designer_id 承担。
- **T4 ✅ 确认**：无 volcano.ts；火山配置在 seedance.ts。
- **T5 ✅ 分工**：db.cjs.initDB（providers/models/media/oss_config/settings/characters 6 表+ALTER）仅 migrate.cjs:4,9 调用；server.js.initDB（L76 定义/L3577 调用，40+ 表，失败 exit(1)）为运行权威。字段已漂移（models 表 server.js 多 mapping_name/credit_cost/supports_reward_balance/reward_credits_required）。
- **T6 ✅ 确认**：modelConfigs = agent 草稿模块，运行时不读（src/ 无 import）；前端读 /api/models。schema：`VideoModelConfig{ modelId; displayName?; providerId?; officialFamily?; docsUrl?; supportedParams: { qualities?; ratios?; durationsSec?(-1=智能); videoModes?; videoResolutions?; maxReferenceImages?; frameRate?; maxNumFrames? }; notes? }`（src/data/modelConfigs/schema.ts）。

## 7. 防复发 A–K 指针
| 出错点 | 主题 | 详见 |
|---|---|---|
| A | 网站打不开先二分 | 03 · Part C / 01 · 铁律 |
| B | SNAKE_MAP 遗漏 | 03 · A9 |
| C | 成败以生成端终态为准(#578) | 03 · A3 |
| D | 双积分 | 03 · A2 |
| E | 删写库后刷新 | 03 · Part C |
| F | 不假百分比 | 03 · Part C |
| G | 图片破图 | 03 · Part C / ui/image.tsx |
| H | 崩溃续轮询 | 03 · A6 |
| I | 取消端点 | 03 · A7 |
| J | 视频 failed 透传+看门狗(#602/#603) | 03 · Part C |
| K | 等待区崩溃恢复(#604) | 03 · A5/A6 |

## 8. 铁律索引
| 铁律 | 落点文件 |
|---|---|
| PG 唯一源 / exit(1) | server.js initDB |
| SNAKE_MAP 双向 | server.js:780 |
| OSS 零二进制 | 前端直传 + OSS |
| 超时以生成端为准 | dispatcher + shared.pollLoop |
| 修复后必验服务启动 | 跨项目纪律 |
| 双余额三段式 | billing.cjs |
| 全局双边记账 | accounting.cjs |
| 支付 fail-closed | payments.cjs / seed-easypay.cjs |
| Git safe-git plumbing | _mk/*.sh / buildtree.cjs |

> 五份文档到此闭环：01 架构链路 → 02 模块职责 → 03 业务规则(🟥)/技术实现(🟩)+出错点 → 04 重构等价验收 → 05 源码索引。
