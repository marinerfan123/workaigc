# 01 · 顶层系统架构

> 本资料库目标：为「同业务完整重构」提供 100% 对齐的基线。
> 五份文档的分工：**01 架构链路（本文）→ 02 模块职责与调用关系 → 03 业务规则（🟥）与技术实现（🟩）+ 出错点 → 04 重构等价验收清单 → 05 源码引用索引**。
>
> 【阅读约定】
> - 🟥 = **业务规则（重构不可改动）**：计费时序、成败判定、幂等、权限、双余额等。
> - 🟩 = **旧技术实现（重构可替换）**：Node 原生 HTTP、内存态集合、localStorage 等。
> - 每条业务结论均附「源码路径 + 行号 + 关键片段」。找不到确证的标注 `【待核验：路径】`。

---

## 0. 一句话定位

墨灵 AI 是一个**异步生成优先**的 AI 生图/生视频 SaaS：前端只做「提交 + 拿三态（pending/done/failed/+waiting/canceled）」，轮询、成败判定、资产落地**全部收归后端**（参照 Replicate / fal.ai / Stability 模式）。PG 是唯一数据源，OSS 只存资产二进制、业务服务器零二进制，SSE 负责近实时终态推送、轮询兜底保证关键路径不丢。

---

## 1. 分层架构（模块作用一览）

```
┌──────────────────────────────────────────────────────────────────────┐
│  前端层（React 19 + Vite 8）                                           │
│   GenerationBar.tsx ── handleGenerate → apiGenerate → useGenerationStream.waitForTask │
│   ui/image.tsx（破图兜底）  OSS 前端直传（成功即落库）                    │
└───────────────┬──────────────────────────────────────────────────────┘
                │ HTTPS (JWT Bearer / cookie 会话)
┌───────────────▼──────────────────────────────────────────────────────┐
│  接入层  server/server.js（Node 原生 HTTP，无框架，3001）              │
│   · 路由分发（/api/generate 等）  · rateLimit  · 幂等查重              │
│   · SNAKE_MAP（PG↔camel 双向契约）  · initDB（PG 唯一源，失败 exit(1)） │
│   · GET /api/generate/stream（SSE 出口）                               │
├──────────────────────────────────────────────────────────────────────┤
│  编排层  dispatcher.cjs（调度核心）                                     │
│   generateAsync → generate → dispatchOne → attemptOnAccount           │
│   → imageGenerate / videoGenerate → providers/video/*                  │
│   · 等待区 / 看门狗 / 崩溃恢复 / 取消信号                              │
├──────────────┬───────────────────────┬───────────────────────────────┤
│ 账务层        │ 适配层                 │ 实时层                         │
│ billing.cjs   │ providers/video/*      │ realtime.cjs                  │
│ (reserve/     │ (agnes/minimax/        │ (EventEmitter 按 user 订阅)   │
│  commit/      │  volcano/generic)      │                               │
│  release)     │                        │                               │
│ accounting.cjs│                        │                               │
│ (双边记账)    │                        │                               │
├──────────────┴───────────────────────┴───────────────────────────────┤
│  持久层 PG17（唯一源） · 缓存 Redis7.2（仅缓存） · 资产 OSS（零二进制） │
└──────────────────────────────────────────────────────────────────────┘
```

设计依据：把「成败判定 / 链接落地 / 重试」收到后端，是为了**让浏览器永不持有 provider.apiKey**（铁律②），并把「崩溃可恢复」作为一等公民（任务 PG 持久化 + 幂等键）。

---

## 2. 核心链路：一次生成请求的跨文件调用图

> 这是全系统最重要的链路。下面按「时间顺序 + 状态传递」展开，每跳都标出**文件:函数(行号)**。

### 2.1 前端提交链（用户点击「生成」）
```
GenerationBar.handleGenerate (src/components/GenerationBar.tsx:994)
  └─ checkBalance()  // 🟥 前端双池余额前置校验（优先赠送→回退充值→都不够拦截），早于占位创建，避免幽灵占位
  └─ 构造 N 个 pending 占位 IMediaItem(status:'pending') → onPendingCreate(pendingIds)  // 🟩 乐观 UI
  └─ crypto.randomUUID() 生成 idempotencyKey  // 🟥 幂等键（后端必需，防网络抖动双扣）
  └─ apiGenerate({model,prompt,ratio,resolution,count,contentType,referenceImages,negative,duration,videoMode,idempotencyKey,pendingIds})
        (src/services/api.ts:442 → apiFetch POST /api/generate)
```

### 2.2 后端接收 + 计费链（server.js）
```
server.js /api/generate POST (server.js:2374)
  ├─ rateLimit('rl:gen:'+ip, 30/60s) → 429  // 🟥 防刷爆配额/积分滥用
  ├─ 校验 realUser（cookie 会话） → 401  // 🟥 必须真实登录才能归属计费/owner
  ├─ 校验 idempotencyKey 非空 → 400  // 🟥 幂等键必需
  ├─ 查同键历史任务 (server.js:2391)
  │    └─ failed → 释放旧 held + 删行腾唯一约束；running/done → 直接返回原 taskId（🟥 绝不重复 reserve）
  ├─ 查 models 计费维度 → billing.resolvePayment(奖励优先/回退充值/都不够抛 INSUFFICIENT|NEED_RECHARGE) (server.js:2409, billing.cjs:11)
  │    └─ 不足 → 402
  ├─ billing.reserveCredits(扣到解析出的池) (server.js:2431, billing.cjs:32)  // 🟥 G3 时序：仅在此扣
  ├─ 组装 genOpts {model,prompt,ratio,resolution,count,contentType,referenceImages,negative,durationSec,videoMode,
  │               user_id,idempotencyKey,cost,costPool,userPlan,clientMeta}  (server.js:2436)
  └─ body.sync ? dispatcher.generate(同步) : dispatcher.generateAsync(pgPool, genOpts)  (server.js:2462 / 2492)
        └─ 返回 {status:'pending', taskId} 给前端
```

**状态传递线路（genOpts）**：`genOpts` 在 server.js 组装后，作为唯一载体一路穿透：
`server.js:2436` → `dispatcher.generateAsync(genOpts:650)` → `generate(runOpts:547)` → `dispatchOne(pairs,tier,input:527)` → `attemptOnAccount(p:448)` → `videoGenerate(input:214)` / `imageGenerate`。
其中 `taskId` 与 `onSubmitted`（视频提交持久化回调）由 `generateAsync` 注入（`runOpts = {...opts, taskId, onSubmitted}`）。

### 2.3 异步编排链（dispatcher.cjs）
```
generateAsync (dispatcher.cjs:650)
  ├─ INSERT generation_tasks(task_id,'running',…,cost,cost_pool,idempotency_key)  // 🟥 PG 持久化（崩溃可捞回）
  ├─ runOpts.onSubmitted = persistProviderTaskId  (dispatcher.cjs:667, 776)
  └─ generate(pgPool, runOpts).then(...)  // 后台跑，不 await
        │
        ├─ 读 settings.app.maxThreads / waitingAreaThreshold (dispatcher.cjs:555)
        ├─ 按 display_name/model_id 解析 models → provider 行，组装 (model×provider) pairs (dispatcher.cjs:565-590)
        │     🟥 多供应商均衡：同一 model_id 下所有 enabled 且 api_key 合法的 provider 都进 pairs
        ├─ 每个 count 起一个 dispatchOne（图片并行 ≤4；视频固定 1）(dispatcher.cjs:594-601)
        │     │
        │     └─ dispatchOne (dispatcher.cjs:527)
        │           └─ 轮询 pairs（RR 均衡），逐个 attemptOnAccount (dispatcher.cjs:533)
        │                 │
        │                 ├─ 账号冷却/桶空/并发满 → 返回 null（静默切下一个）
        │                 └─ attemptOnAccount (dispatcher.cjs:448)
        │                       ├─ 视频 → videoGenerate(p.provider,p.model,input) (dispatcher.cjs:463)
        │                       │     ├─ key≠'generic' → providers/video/index.cjs.submit → onSubmitted 持久化 providerTaskId
        │                       │     │                 → providers/video/index.cjs.poll（续轮询，isCancelled=cancelledTasks.has）
        │                       │     └─ key==='generic' → 内联 submit+poll，deadline=90min 安全线 (dispatcher.cjs:266)
        │                       └─ 图片 → imageGenerate (dispatcher.cjs:129)
        │
        └─ .then(result) 终态处理（见 2.4）
```

### 2.4 终态链（成败判定 + 计费结算 + 推送）
```
generate().then(result) (dispatcher.cjs:671)
  ├─ cancelledTasks.has(taskId) → 取消护栏：标 canceled，不 commit（覆盖图片无 poll 路径）(dispatcher.cjs:674)
  ├─ result.status==='success' (dispatcher.cjs:684)
  │     ├─ billing.commitCredits(pgPool,user_id,cost,idempotencyKey,costPool)  // 🟥 G3 结算点
  │     ├─ accounting.recordConsumption(双边记账：按 provider/model 分摊)  // 🟥 全局无例外
  │     ├─ UPDATE generation_tasks SET status='done'  (dispatcher.cjs:703)
  │     └─ realtime.emitTaskUpdate(user_id,{taskId,status:'done',result})  // 推 SSE
  ├─ result.status==='throttled' → enqueueWaiting + persistWaitingOpts(崩溃恢复) + runWaitingPump (dispatcher.cjs:709-718)
  ├─ result.status==='canceled' → 幂等兜底标 canceled（权威终态已由 cancelTask 完成）(dispatcher.cjs:719)
  ├─ result.status==='timeout' → UPDATE status='waiting'（🟥 绝不判失败、绝不释放积分，保留待复核）(dispatcher.cjs:725)
  └─ else(failed/error) → billing.releaseCredits（释放 held）+ UPDATE status='failed' + logError（🟥 落核心错误日志）(dispatcher.cjs:731-747)
```

### 2.5 前端接收终态链
```
realtime.emitTaskUpdate → server.js SSE /api/generate/stream 推送 data: JSON (server.js:2535)
  → 前端 EventSource.onmessage → listeners.get(taskId) 回调 (useGenerationStream.ts:36)
  → 或 3s 轮询兜底 apiGetGenerationStatus 命中终态 (useGenerationStream.ts:88)
  → waitForTask resolve → GenerationBar.pollTaskUntilDone → processResultImages（前端写 media + OSS 直传）(GenerationBar.tsx:522, 416)
```

**关键状态传递线路总览**
| 状态/信号 | 载体 | 跨越边界 | 用途 |
|---|---|---|---|
| genOpts | JS 对象 | server→dispatcher→attemptOnAccount→videoGenerate | 贯穿计费/参数 |
| taskId | DB 列 + 内存 | 前端↔server↔dispatcher | 关联任务与占位 |
| onSubmitted | 闭包回调 | generateAsync→videoGenerate→persistProviderTaskId | 视频提交即落 providerTaskId |
| cancelledTasks | 内存 Set | cancelTask→pollLoop/generateAsync.then/finalizeResumedTask | 取消信号（进程重启清空） |
| resume_meta | PG jsonb | 崩溃前→重启后 resumeWaitingArea/resumeOneTask | 等待区/续轮询重建 |
| SSE payload | EventEmitter→res | dispatcher→前端 | 近实时终态 |

---

## 3. 各层设计依据（为什么这样设计）

- **为什么异步主流化**：浏览器不持有 apiKey（铁律②）；崩溃可由 PG + 幂等键捞回；前端只维护三态，逻辑极简。
- **为什么 PG 是唯一数据源**：initDB 连 PG 失败→重试5次→exit(1)，绝不许走 JSON（铁律）。所有任务/用户/模型/供应商/账务都以 PG 为准，Redis 仅缓存。
- **为什么 OSS 零二进制**：业务服务器不存任何二进制，只持预签名 URL；用户资产（图/视频）成功即传 OSS 并落 `oss_url`（铁律④，已落地）。
- **为什么 SSE + 轮询兜底**：生成完成是关键路径，绝不能因 SSE 抖动丢失；故 `waitForTask` 同时监听 SSE 与每 3s 轮询，任一命中即 resolve（useGenerationStream.ts:60）。
- **为什么成败只听生成端终态**：固定时间只作防僵尸安全线（默认 90min），超时绝不判失败 / 绝不退积分，标 `timeout`/`waiting` 保留待复核（铁律·超时，#578）。
- **为什么等待区 + 看门狗**：某任务所有供应商都不可用时进等待区后台重试（不立即失败）；>3h 孤儿 running 由看门狗强制 failed+释放（仅回收 running，不碰 waiting）。

---

## 4. 综合：用户约定与铁律索引（详细见 03）

| 类别 | 条目 | 落点 |
|---|---|---|
| 架构铁律 | PG 唯一源 / SNAKE_MAP / OSS 零二进制 / 超时以生成端为准 / 修复后必验服务启动 | 03 · 铁律章 |
| 账务铁律 | 双余额 + 三段式 reserve/commit/release + 全局双边记账 | 03 · A1/A2/A11 |
| 支付安全 | 易支付 fail-closed + webhook 四关 + AES-256-GCM | 03 · A10 |
| 出错点 | A 打不开先二分 / B SNAKE_MAP 遗漏 / C 终态为准 / D 双积分 / E 删写库后刷新 / F 不假百分比 / G 破图 / H 崩溃续轮询 / I 取消 / J 视频 failed 透传+看门狗 / K 等待区崩溃恢复 | 03 · Part C |

> 下一步读 **02_全模块职责说明**（每个模块的作用 + 跨文件调用关系 + 末尾「约定/出错点 → 模块」映射）。
