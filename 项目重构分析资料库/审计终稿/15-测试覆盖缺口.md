# 15 - 测试覆盖缺口

> 审计对象：墨灵 AI（E:\code）
> 审计性质：**只读**；结论以源码/配置文件为准。未运行任何写入/变更命令（测试未执行，状态以"设计意图 + 源码推断"标注）。
> 证据等级图例：【源码已证实】【运行时已证实】【数据库已证实】【测试已证实】【文档记载但源码未证实】【推断】【未知·待核验】

---

## 1. 现有测试全盘点数（主项目，不含 refs/ 参考代码）

> 主项目共 **8 个测试文件**（已用 Glob 全量盘点，排除 `refs/`、`node_modules/`、`dist/`）。
> **全部 8 个文件均使用 `node:test`**（server 端 `.cjs` 用 `require('node:test')`，前端 `.ts` 用 `import { describe, it } from 'node:test'`），**无任何文件使用 vitest**（无 `from 'vitest'` 导入）。

| # | 文件路径 | 框架 | 覆盖模块 | 是否通过（本审计未执行） | 备注 |
|---|---------|------|---------|------------------------|------|
| 1 | `server/modules/modelhub/resolver.test.cjs` | node:test | modelhub resolver（模型身份解析：model_id 优先 / display_name 回退 / 去重 / enabled 过滤 / 兜底） | 未执行；设计以 `node --test` 运行，应通过 | 内存 fake pool，不连 PG；含与 dispatcher 的契约测试 |
| 2 | `server/modules/modelhub/bindings.test.cjs` | node:test | dispatch pair 构建（bindings 优先 / 双读回退 / 禁用 provider·绑定过滤 / 短 api_key 过滤） | 未执行；设计以 `node --test` 运行，应通过 | 内存 fake pool；含契约测试 |
| 3 | `src/__tests__/data/characters.test.ts` | node:test | 静态数据 `data/characters.ts`（`MOCK_CHARACTERS` 形状 / 类型） | 未执行；断言静态常量与类型 | 无业务逻辑 |
| 4 | `src/__tests__/data/media.test.ts` | node:test | 静态数据 `data/media.ts`（`MOCK_MEDIA_LIST` / `IMediaItem` 类型校验） | 同 | 无业务逻辑 |
| 5 | `src/__tests__/data/models.test.ts` | node:test | 静态数据 `data/models.ts`（`MOCK_MODELS` / `MOCK_PROVIDERS` / `PROVIDER_TEMPLATES`） | 同 | 无业务逻辑 |
| 6 | `src/__tests__/data/oss.test.ts` | node:test | `data/oss.ts` 默认配置结构（含 `accessKeyId/Secret` 默认空断言） | 同 | 仅结构/默认值，不测加密/上传 |
| 7 | `src/__tests__/data/settings.test.ts` | node:test | `data/settings.ts`（`DEFAULT_SETTINGS` / `IGenerationSettings` 类型） | 同 | 无业务逻辑 |
| 8 | `src/__tests__/lib/utils.test.ts` | node:test | `lib/utils.ts` 的 `cn()` 类名合并（手写 mock 逻辑，非调用真实 clsx/twMerge） | 同 | 未真正覆盖 twMerge 冲突解析 |

> 说明：refs/ 下存在大量 `*.spec.ts` / `*.test.ts`（sub2api、sub2apipay 等参考实现），属**参考代码**而非本应用，不计入本表。

---

## 2. 框架不一致（已证实的高优先级问题）

| 项 | 事实 | 证据 | 置信度 |
|----|------|------|-------|
| `npm test` 命令 | `package.json:13` `"test": "vitest run"` | 【源码已证实】 | 高 |
| 实际测试框架 | 全部 8 个测试文件用 `node:test`，**无 vitest 配置**（`vite.config.ts` 无 `test` 块，无 `vitest.config.*`） | 【源码已证实】 | 高 |
| 冲突后果 | `vitest run` 默认扫描 `*.test.*`，但 src 测试 `import ... from 'node:test'` 且直接 `import ... from '../../data/oss.ts'`（需 TS loader）；vitest 不识别 node:test 注册器 → **`npm test` 实际跑不到这 8 个用例（或 0 通过）**。这些文件头注释明确要求 `node --test` 运行 | 【源码已证实】 | 高 |
| 结论 | **测试脚本与测试代码框架不匹配**：`test=vitest` 但代码是 `node:test`。重构前必须二选一统一（建议统一为 vitest 并补 `test` 配置，或将脚本改为 `node --test`）。 | 【源码已证实】 | 高 |

---

## 3. 关键未覆盖模块

| 模块 | 文件 | 当前测试 | 风险说明 | 证据 |
|------|------|---------|---------|------|
| 计费 | `server/billing.cjs` | 无 | 余额扣减/释放/对账，含幂等 TOCTOU 隐患 | 【源码已证实】 |
| 账务（双边记账） | `server/accounting.cjs` | 无 | `consumption_ledger` 写入，幂等键非唯一 | 【源码已证实】 |
| 调度 | `server/dispatcher.cjs`（76KB） | 仅 resolver/bindings 单元（上游依赖） | 状态机转换、resume、终态、限流令牌桶无测试 | 【源码已证实】 |
| 支付 | `server/payments.cjs` + `payments/*` | 无 | 订单创建/校验/webhook 验签/幂等入账无测试 | 【源码已证实】 |
| 支付加密 | `server/payments/crypto.cjs` | 无 | AES-256-GCM 加密/解密/脱敏无测试（虽 fail-closed 重要） | 【源码已证实】 |
| 中央 API handler | `server/server.js handleAPI`（L1573） | 无 | 路由分发、鉴权、错误契约无测试 | 【源码已证实】 |
| 鉴权 | `server/auth.cjs` | 无 | HMAC-SHA256 JWT 签发/验签、IDOR 边界无测试 | 【源码已证实】 |
| 前端业务 | `src/`（React 组件/页面/store） | 仅 6 个静态数据/工具测试 | 业务交互、SSE、表单、支付流 UI 无测试 | 【源码已证实】 |
| Redis 降级 | `server/redis.cjs` | 无 | 降级到内存 Map 的行为无测试 | 【源码已证实】 |
| SSE 契约 | `realtime.cjs` / `oss-logger.cjs` | 无 | 事件字段契约无测试 | 【源码已证实】 |

---

## 4. P0 测试缺口（重构前必须补的字符化测试）

以下缺口均以源码为证（非推断）：

| # | 缺口 | 源码证据 | 危害 | 置信度 |
|---|------|---------|------|-------|
| P0-1 | **计费幂等（credit_transactions 缺 `(ref,kind)` 唯一约束的 TOCTOU 双退）** | `billing.cjs:48` `_hasPosted` 先 `SELECT` 再 `INSERT`；`credit_transactions` 定义仅 `INDEX ix_ct_ref`（server.js:268–278），**无 `(ref,kind)` 唯一约束** | 并发/崩溃重试下 `release`/`commit` 双记账 → 用户余额被多退 | 【源码已证实】高 |
| P0-2 | **consumption_ledger 幂等键非唯一 → 并发双记账** | `accounting.cjs:63` 先 `SELECT idempotency_key` 再 `INSERT`；`consumption_ledger` 仅 `INDEX ix_cl_idem`（server.js:686），**无 UNIQUE** | 同一消耗被记两笔 → 经营看板 margin 失真、成本虚高 | 【源码已证实】高 |
| P0-3 | **IDOR（越权访问他人资源）** | `billing.cjs` / `payments.cjs` 订单查询多按 `user_id=$1 AND ...` 过滤，但需测试覆盖所有 `/api/*` 是否强制归属校验（如 `getOrder` L199 已校验，其它路径待验证） | 用户枚举/篡改他人订单、任务、媒体 | 【源码已证实】中（需逐路由验证） |
| P0-4 | **任务状态机转换** | `dispatcher.cjs` 状态：`pending→running→success/failed/timeout/canceled`；多处 UPDATE（L700/753/777/943） | 非法状态跃迁（如 success 后再次 success）导致重复 commit/release | 【源码已证实】高 |
| P0-5 | **SSE 字段契约** | `oss-logger.cjs:84` `data: ${JSON.stringify(msg)}`；`realtime.cjs` 事件 | 前端强依赖 `{type,data}`；字段变更无契约测试会静默断连 | 【源码已证实】中 |

> 注：`generation_tasks.idempotency_key` **已建唯一索引** `ux_gt_idem`（server.js:301），任务级幂等由 DB 强制，故"任务重复创建"缺口较低；但 **credit_transactions / consumption_ledger 仍缺唯一约束**，幂等仅靠应用层 SELECT-then-INSERT（TOCTOU），为最高危。

---

## 5. 建议的最小字符化测试集（重构前补）

> 目标：用最小成本锁定"资金/账务正确性"与"越权"两条生命线。统一框架（见 §2）后落地。

1. **`billing.idempotency.test.cjs`**：并发 2 笔同 `ref` 的 `releaseCredits` / `commitCredits`，断言 `credit_transactions` 仅 1 行（暴露 P0-1 TOCTOU）。
2. **`accounting.idempotency.test.cjs`**：并发同 `idempotencyKey` 的 `recordConsumption`，断言 `consumption_ledger` 仅 1 行（暴露 P0-2）。
3. **`dispatcher.statemachine.test.cjs`**：驱动非法状态跃迁（success→success、failed→running），断言不重复 commit/release。
4. **`payments.webhook.test.cjs`**：构造重复 webhook 回调，断言仅入账一次（幂等表 `webhook_events` UNIQUE 已建 server.js:574，应验证）。
5. **`auth.jwt.test.cjs`**：签发→篡改→验签失败；缺 `JWT_SECRET` 回退值不可用于生产断言。
6. **`idOR.test.cjs`**：跨用户访问 `/api/credits/orders/:no`、`/api/media/:id` 等，断言 403/404。
7. **`crypto.aes256gcm.test.cjs`**：encrypt→decrypt 往返、错误主密钥解密失败、脱敏格式。
8. **`redis.fallback.test.cjs`**：模拟 Redis 断开，断言 `kvGet/Set/Incr` 降级内存且行为一致。

---

## 6. 审计发现摘要（最关键风险 5 条）

1. **测试框架与脚本不匹配**：`npm test`=vitest，但全部 8 个测试用 `node:test`、无 vitest 配置，`npm test` 实际跑不到这些用例。【源码已证实·高】
2. **资金幂等缺口（最高危）**：`credit_transactions` 缺 `(ref,kind)` 唯一约束、`consumption_ledger` 幂等键非唯一，均靠 SELECT-then-INSERT（TOCTOU），并发/崩溃重试可双退/双记账。【源码已证实·高】
3. **核心资金模块零测试**：billing / accounting / payments / crypto 无任何测试覆盖，重构无安全网。【源码已证实·高】
4. **调度与 API handler 无测试**：dispatcher.cjs（76KB）状态机、resume、server.js handleAPI 均无测试，重构易引入回归。【源码已证实·高】
5. **前端业务零测试**：src 仅 6 个静态数据/工具测试，React 业务组件、SSE、支付流 UI 无覆盖。【源码已证实·高】
